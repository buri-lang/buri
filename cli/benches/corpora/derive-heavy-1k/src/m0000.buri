//! Generated module 0 of the compiler benchmark corpus.
//!
//! Emitted by `cli/benches/generate.rs` from a fixed seed. Nothing in
//! here is meant to be read; it is meant to be *compiled*, at a
//! controlled size and with the construct mix of real source.

from "core/str" import * as str;
from "core/list" import * as list;
from "core/effect" import { Alloc };
from "core/json" import { ToJson, FromJson };

/// The record every module in this corpus exports, so that an import
/// of any module resolves to a known shape.
export struct Node0 {
  export label: Str,
  export value: Int,
  export weight: Float,
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for Node0;

export fn seed0(): Int { 1 }

/// Folds this module's seed together with its dependencies'.
export fn blend0(base: Int): Int {
  base + seed0()
}

/// A fold and a filter over a literal list, with lambdas — closures
/// are their own path in both the checker and the backend.
export fn digest0_0<C: Alloc>(ctx: C): Int {
  let xs: [Int] = [4887, 4492, 6163, 9249, 6360];
  let kept = xs.filter(ctx, fn(x) => x % 2 == 0);
  let total = kept.fold(fn(acc, x) => acc + x, 0);
  total + xs.len()
}

/// An empty list of an explicit type, which is the other side of
/// inference: nothing constrains the element type but the annotation.
export fn drain0_0(): Int {
  let empty: [Int] = list.empty<Int>();
  empty.len()
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_0<C: Alloc>(ctx: C): Int {
  digest0_0(ctx) + drain0_0()
}

/// A record with 2 fields, derived comparisons and two methods.
export struct Rec0_1 {
  export f0: Int,
  export f1: Str,
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for Rec0_1;

impl Rec0_1 {
  /// A pure accessor, of the kind that is most of a real impl block.
  export fn scaled(self, factor: Int): Int {
    self.f0 * factor + 893
  }

  /// Allocates, and says so with `C: Alloc`.
  export fn render<C: Alloc>(self, ctx: C): Str {
    str.format(ctx, "rec0_1: ${self.f0}/${self.f1}")
  }
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_1<C: Alloc>(ctx: C): Int {
  let r = Rec0_1 { f0: 7, f1: "probe" };
  r.scaled(3) + r.render(ctx).len()
}

/// An enum of 6 variants, matched exhaustively below.
export enum State0_2 {
  export Idle,
  export Running(Int),
  export Failed { code: Int, message: Str },
  export Step3(Int, Str),
  export Step4(Int, Str),
  export Step5(Int, Str),
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for State0_2;

/// The match every enum in a real program has somewhere.
export fn rank0_2(state: State0_2): Int {
  match (state) {
    .Idle => 0,
    .Running(ticks) => ticks + 1,
    .Failed { code, .. } => code * -1,
    .Step3(k, _) => k + 3,
    .Step4(k, _) => k + 4,
    .Step5(k, _) => k + 5,
  }
}

/// Construction, so the variants are reachable from a root.
export fn start0_2(ticks: Int): State0_2 {
  if (ticks == 0) { .Idle } else if (ticks < 38) { .Running(ticks) }
  else { .Failed { code: ticks, message: "overrun" } }
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_2<C: Alloc>(ctx: C): Int {
  let s = start0_2(5);
  rank0_2(s) + s.show(ctx).len()
}

/// String building through interpolation, which is a template
/// literal in the lexer and a `format` call in the backend.
export fn label0_3<C: Alloc>(ctx: C, id: Int, name: Str): Str {
  let prefix = if (id < 0) { "echo" } else { "bravo" };
  let body = str.format(ctx, "${prefix}-${name}-${id}");
  body.toUpper(ctx)
}

/// Literals of the kinds the lexer has separate paths for: decimal,
/// hexadecimal, float, char, escaped string.
export fn constants0_3(): (Int, Int, Float, Char, Str) {
  (440017, 0xED10, 743.983, 'q', "a\ttab and a \"quote\"")
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_3<C: Alloc>(ctx: C): Int {
  let (a, b, _, _, e) = constants0_3();
  label0_3(ctx, a, e).len() + b
}

/// An enum of 7 variants, matched exhaustively below.
export enum State0_4 {
  export Idle,
  export Running(Int),
  export Failed { code: Int, message: Str },
  export Step3(Int, Str),
  export Step4(Int, Str),
  export Step5(Int, Str),
  export Step6(Int, Str),
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for State0_4;

/// The match every enum in a real program has somewhere.
export fn rank0_4(state: State0_4): Int {
  match (state) {
    .Idle => 0,
    .Running(ticks) => ticks + 1,
    .Failed { code, .. } => code * -1,
    .Step3(k, _) => k + 3,
    .Step4(k, _) => k + 4,
    .Step5(k, _) => k + 5,
    .Step6(k, _) => k + 6,
  }
}

/// Construction, so the variants are reachable from a root.
export fn start0_4(ticks: Int): State0_4 {
  if (ticks == 0) { .Idle } else if (ticks < 13) { .Running(ticks) }
  else { .Failed { code: ticks, message: "overrun" } }
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_4<C: Alloc>(ctx: C): Int {
  let s = start0_4(5);
  rank0_4(s) + s.show(ctx).len()
}

/// A record with 5 fields, derived comparisons and two methods.
export struct Rec0_5 {
  export f0: Int,
  export f1: Str,
  export f2: Float,
  export f3: Bool,
  export f4: Int,
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for Rec0_5;

impl Rec0_5 {
  /// A pure accessor, of the kind that is most of a real impl block.
  export fn scaled(self, factor: Int): Int {
    self.f0 * factor + 963
  }

  /// Allocates, and says so with `C: Alloc`.
  export fn render<C: Alloc>(self, ctx: C): Str {
    str.format(ctx, "rec0_5: ${self.f0}/${self.f1}")
  }
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_5<C: Alloc>(ctx: C): Int {
  let r = Rec0_5 { f0: 7, f1: "probe", f2: 1.5, f3: true, f4: 7 };
  r.scaled(3) + r.render(ctx).len()
}

/// A fold and a filter over a literal list, with lambdas — closures
/// are their own path in both the checker and the backend.
export fn digest0_6<C: Alloc>(ctx: C): Int {
  let xs: [Int] = [9911, 5863, 8785, 2858, 2247, 9325, 7428, 5205, 1220, 3379];
  let kept = xs.filter(ctx, fn(x) => x % 2 == 0);
  let total = kept.fold(fn(acc, x) => acc + x, 0);
  total + xs.len()
}

/// An empty list of an explicit type, which is the other side of
/// inference: nothing constrains the element type but the annotation.
export fn drain0_6(): Int {
  let empty: [Int] = list.empty<Int>();
  empty.len()
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_6<C: Alloc>(ctx: C): Int {
  digest0_6(ctx) + drain0_6()
}

/// A fold and a filter over a literal list, with lambdas — closures
/// are their own path in both the checker and the backend.
export fn digest0_7<C: Alloc>(ctx: C): Int {
  let xs: [Int] = [6308, 9379, 262, 1021, 8784, 2362];
  let kept = xs.filter(ctx, fn(x) => x % 2 == 0);
  let total = kept.fold(fn(acc, x) => acc + x, 0);
  total + xs.len()
}

/// An empty list of an explicit type, which is the other side of
/// inference: nothing constrains the element type but the annotation.
export fn drain0_7(): Int {
  let empty: [Int] = list.empty<Int>();
  empty.len()
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_7<C: Alloc>(ctx: C): Int {
  digest0_7(ctx) + drain0_7()
}

/// An enum of 6 variants, matched exhaustively below.
export enum State0_8 {
  export Idle,
  export Running(Int),
  export Failed { code: Int, message: Str },
  export Step3(Int, Str),
  export Step4(Int, Str),
  export Step5(Int, Str),
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for State0_8;

/// The match every enum in a real program has somewhere.
export fn rank0_8(state: State0_8): Int {
  match (state) {
    .Idle => 0,
    .Running(ticks) => ticks + 1,
    .Failed { code, .. } => code * -1,
    .Step3(k, _) => k + 3,
    .Step4(k, _) => k + 4,
    .Step5(k, _) => k + 5,
  }
}

/// Construction, so the variants are reachable from a root.
export fn start0_8(ticks: Int): State0_8 {
  if (ticks == 0) { .Idle } else if (ticks < 70) { .Running(ticks) }
  else { .Failed { code: ticks, message: "overrun" } }
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_8<C: Alloc>(ctx: C): Int {
  let s = start0_8(5);
  rank0_8(s) + s.show(ctx).len()
}

/// An enum of 5 variants, matched exhaustively below.
export enum State0_9 {
  export Idle,
  export Running(Int),
  export Failed { code: Int, message: Str },
  export Step3(Int, Str),
  export Step4(Int, Str),
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for State0_9;

/// The match every enum in a real program has somewhere.
export fn rank0_9(state: State0_9): Int {
  match (state) {
    .Idle => 0,
    .Running(ticks) => ticks + 1,
    .Failed { code, .. } => code * -1,
    .Step3(k, _) => k + 3,
    .Step4(k, _) => k + 4,
  }
}

/// Construction, so the variants are reachable from a root.
export fn start0_9(ticks: Int): State0_9 {
  if (ticks == 0) { .Idle } else if (ticks < 91) { .Running(ticks) }
  else { .Failed { code: ticks, message: "overrun" } }
}

/// Reaches everything above from one call. See `reach` below.
export fn probe0_9<C: Alloc>(ctx: C): Int {
  let s = start0_9(5);
  rank0_9(s) + s.show(ctx).len()
}

/// Every declaration in this module, reachable from one call, so that
/// monomorphization has to visit the whole of it.
export fn reach0<C: Alloc>(ctx: C): Int {
  0
    + probe0_0(ctx)
    + probe0_1(ctx)
    + probe0_2(ctx)
    + probe0_3(ctx)
    + probe0_4(ctx)
    + probe0_5(ctx)
    + probe0_6(ctx)
    + probe0_7(ctx)
    + probe0_8(ctx)
    + probe0_9(ctx)
}
