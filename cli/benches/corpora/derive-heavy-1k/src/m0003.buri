//! Generated module 3 of the compiler benchmark corpus.
//!
//! Emitted by `cli/benches/generate.rs` from a fixed seed. Nothing in
//! here is meant to be read; it is meant to be *compiled*, at a
//! controlled size and with the construct mix of real source.

from "core/effect" import { Alloc };
from "core/json" import { FromJson, ToJson };
from "core/list" import * as list;
from "core/str" import * as str;
from "//bench/m0000.buri" import { blend0, Node0, reach0, seed0 };
from "//bench/m0002.buri" import { blend2, Node2, reach2, seed2 };

derive Eq, Show, Ord, Hash, ToJson, FromJson for Node3;
/// The record every module in this corpus exports, so that an import
/// of any module resolves to a known shape.
export struct Node3 {
    export label: Str,
    export value: Int,
    export weight: Float,
}

export fn seed3(): Int {
    3758
}

/// Folds this module's seed together with its dependencies'.
export fn blend3(base: Int): Int {
    let d0 = blend2(base) + seed2();
    let d1 = blend0(base) + seed0();
    d0 + d1 + seed3()
}

/// Reads a dependency's record, so the import carries a type as
/// well as a function.
export fn lift3(n: Node2): Node3 {
    Node3 { label: n.label, value: n.value + seed3(), weight: n.weight }
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for State3_0;
/// An enum of 6 variants, matched exhaustively below.
export enum State3_0 {
    Idle,
    Running(Int),
    Failed { code: Int, message: Str },
    Step3(Int, Str),
    Step4(Int, Str),
    Step5(Int, Str),
}

/// The match every enum in a real program has somewhere.
export fn rank3_0(state: State3_0): Int {
    match (state) {
        .Idle => 0,
        .Running(ticks) => ticks + 1,
        .Failed { code, .. } => code * -1,
        .Step3(k, _) => k + 3,
        .Step4(k, _) => k + 4,
        .Step5(k, _) => k + 5,
    }
}

/// Construction, so the variants are reachable from a root.
export fn start3_0(ticks: Int): State3_0 {
    if (ticks == 0) {
        .Idle
    } else if (ticks < 98) {
        .Running(ticks)
    } else {
        .Failed { code: ticks, message: "overrun" }
    }
}

/// Reaches everything above from one call. See `reach` below.
export fn probe3_0<C: Alloc>(ctx: C): Int {
    let s = start3_0(5);
    rank3_0(s) + s.show(ctx).len()
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for Rec3_1;
/// A record with 3 fields, derived comparisons and two methods.
export struct Rec3_1 {
    export f0: Int,
    export f1: Str,
    export f2: Float,
}

impl Rec3_1 {
    /// A pure accessor, of the kind that is most of a real impl block.
    export fn scaled(self, factor: Int): Int {
        self.f0 * factor + 107
    }

    /// Allocates, and says so with `C: Alloc`.
    export fn render<C: Alloc>(self, ctx: C): Str {
        str.format(ctx, "rec3_1: ${self.f0}/${self.f1}")
    }
}

/// Reaches everything above from one call. See `reach` below.
export fn probe3_1<C: Alloc>(ctx: C): Int {
    let r = Rec3_1 { f0: 7, f1: "probe", f2: 1.5 };
    r.scaled(3) + r.render(ctx).len()
}

/// A fold and a filter over a literal list, with lambdas — closures
/// are their own path in both the checker and the backend.
export fn digest3_2<C: Alloc>(ctx: C): Int {
    let xs: [Int] = [3497, 2536, 5521, 2, 7397, 3954, 3671, 9413, 7825, 1526];
    let kept = xs.filter(ctx, fn(x) => x % 2 == 0);
    let total = kept.fold(fn(acc, x) => acc + x, 0);
    total + xs.len()
}

/// An empty list of an explicit type, which is the other side of
/// inference: nothing constrains the element type but the annotation.
export fn drain3_2(): Int {
    let empty: [Int] = list.empty<Int>();
    empty.len()
}

/// Reaches everything above from one call. See `reach` below.
export fn probe3_2<C: Alloc>(ctx: C): Int {
    digest3_2(ctx) + drain3_2()
}

derive Eq, Show, Ord, Hash, ToJson, FromJson for State3_3;
/// An enum of 7 variants, matched exhaustively below.
export enum State3_3 {
    Idle,
    Running(Int),
    Failed { code: Int, message: Str },
    Step3(Int, Str),
    Step4(Int, Str),
    Step5(Int, Str),
    Step6(Int, Str),
}

/// The match every enum in a real program has somewhere.
export fn rank3_3(state: State3_3): Int {
    match (state) {
        .Idle => 0,
        .Running(ticks) => ticks + 1,
        .Failed { code, .. } => code * -1,
        .Step3(k, _) => k + 3,
        .Step4(k, _) => k + 4,
        .Step5(k, _) => k + 5,
        .Step6(k, _) => k + 6,
    }
}

/// Construction, so the variants are reachable from a root.
export fn start3_3(ticks: Int): State3_3 {
    if (ticks == 0) {
        .Idle
    } else if (ticks < 49) {
        .Running(ticks)
    } else {
        .Failed { code: ticks, message: "overrun" }
    }
}

/// Reaches everything above from one call. See `reach` below.
export fn probe3_3<C: Alloc>(ctx: C): Int {
    let s = start3_3(5);
    rank3_3(s) + s.show(ctx).len()
}

/// Every declaration in this module, reachable from one call, so that
/// monomorphization has to visit the whole of it.
export fn reach3<C: Alloc>(ctx: C): Int {
    0
        + probe3_0(ctx)
        + probe3_1(ctx)
        + probe3_2(ctx)
        + probe3_3(ctx)
        + reach2(ctx)
        + reach0(ctx)
}
