// CRASH: shift out of range
// A negative shift has no answer either, and is rejected by the same guard as
// a shift at or beyond the width.
from "core/bits" import * as bits;
from "core/effect" import { Allocator, Stdout, Environment };
from "core/env" import * as env;
from "core/host" import * as host;
from "core/io" import * as io;

fn push(x: Int, n: Int): Int { bits.shiftLeft(x, n) }

export fn main(): Result<(), Str> {
  let ctx = context { Allocator: host.alloc, Stdout: host.stdout, Environment: host.env };
  let count = 0 - env.arguments(ctx).length() - 1;
  let _ = io.println(ctx, "${push(1, count)}").ignore();
  .Ok(())
}
