// CRASH: division by zero
//
// `number.divideEuclidean` is written in terms of `/` and `%`, so a zero divisor
// aborts with the message those abort with rather than answering an `Option`
// nobody asked for (`number.buri`, SPEC 6.2).
from "core/effect" import { Allocator, Stdout, Environment };
from "core/env" import * as env;
from "core/host" import * as host;
from "core/io" import * as io;
from "core/number" import * as number;

export fn main(): Result<(), Str> {
  let ctx = context { Allocator: host.alloc, Stdout: host.stdout, Environment: host.env };
  let zero = env.arguments(ctx).length();
  let _ = io.println(ctx, "${number.divideEuclidean(-7, zero)}").ignore();
  .Ok(())
}
