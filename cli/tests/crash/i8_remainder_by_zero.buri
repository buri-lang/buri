// CRASH: division by zero
// `%` at a narrow width, which is a different code path from `Int`.
from "core/effect" import { Allocator, Stdout, Environment };
from "core/env" import * as env;
from "core/host" import * as host;
from "core/io" import * as io;

fn leftover(a: I8, b: I8): I8 { a % b }

export fn main(): Result<(), Str> {
  let ctx = context { Allocator: host.alloc, Stdout: host.stdout, Environment: host.env };
  let zero: I8 = env.arguments(ctx).length().toI8().withDefault(0);
  let _ = io.println(ctx, "${leftover(7, zero)}").ignore();
  .Ok(())
}
