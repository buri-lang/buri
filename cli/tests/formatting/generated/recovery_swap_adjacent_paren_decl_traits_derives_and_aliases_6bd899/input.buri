export type Pairing<T> = (TT ,);
export struct Meters(export Float);
derive Eq, Ord, Show, ToJson, FromJson, Hash, Clone, Default, Debug, Display for Meters;
export effect Clock { fn now(self): Int; }
