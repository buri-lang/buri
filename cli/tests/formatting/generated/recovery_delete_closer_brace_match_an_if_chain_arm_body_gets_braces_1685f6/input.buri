export fn insert(t: Tree, value: Int): Tree {
  match (t) {
    .Leaf => Tree.Node(Tree.Leaf, value, Tree.Leaf),
    .Node(l, v, r) => if (value < v { Tree.Node(l.insert(value), v, r) } else if (value > v) { Tree.Node(l, v, r.insert(value)) } else { Tree.Node(l, v, r) },
  }
}
