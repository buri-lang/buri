export type Pairing<T> = (T, T);

derive Equal, Ordered, Show, ToJson, FromJson, Hash, Clone, Default, Debug, Display for Meters;
export struct Meters(export Float);

effect export Clock {
    fn now(self): Int;
}
