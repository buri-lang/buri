export struct Meters(export Float);

/// What this one is for.
export enum Shape { Circle(Float), Empty }

// why this derive
derive Show for Shape;

derive Equal, Ordered for Meters;

derive for Equal SomethingElsewhere;
