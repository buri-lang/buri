error: `T` has no method `len` [no-such-method]
 --> cmd/method_on_unbounded_type_param/main.buri:6:5
  |
6 |   x.len()
  |     ^^^
  |
  = `T` carries no bounds, and a bare type parameter has no methods; add a bound that declares `len`
  = fix: check the spelling, or declare it in `impl T { ... }` in that type's own module — a method may not be added from anywhere else

    What to do
    ──────────

    Check the spelling, or declare it in an impl block in that type's own
    module.

    Why
    ───

    A method is looked up in exactly one place: the module that declares the
    receiver's type. There is no extension mechanism, so a method cannot be
    added to a type from outside — which is also what makes resolution a single
    lookup rather than a search.

