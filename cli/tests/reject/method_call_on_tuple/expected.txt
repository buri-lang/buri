error: `(I64, I64)` has no method `length` [no-such-method]
 --> cmd/method_call_on_tuple/main.buri:7:5
  |
7 |   t.length()
  |     ^^^^^^
  |
  = tuples, function types, and `Template` have no defining module, so they have no methods
  = fix: there is no module to declare `length` in, so write a free function taking the value, or wrap it in a struct of yours and give that the method

    Why
    ───

    A method is looked up in exactly one place: the module that declares the
    receiver's type. There is no extension mechanism, so nobody can add a method
    to a type from outside — which is why the fix points a toolchain type at its
    page rather than at an impl block you could not write.

