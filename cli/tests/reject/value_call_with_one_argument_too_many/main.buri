// EXPECT: expected 1 arguments, found 2
//
// Nothing is missing, so no position is named: the fix prints the type and
// leaves the choice of which argument to drop where it belongs.
fn go(): Int {
  let f = fn(a: Int): Int => a;
  f(1, 2)
}
