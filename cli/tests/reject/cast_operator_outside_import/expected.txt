error: expected `;`, found `as` [unexpected-token]
 --> cmd/cast_operator_outside_import/main.buri:8:13
  |
8 |   let d = 5 as U8;
  |             ^^
  |
  = expected: `;`
  = actual: `as`
  = fix: write `;` here

    What to do
    ──────────

    Write what the message names. This is the parser's catch-all, so the useful
    half of it is always the "expected" part. A mistake the parser can name — a
    separator, a terminator, an arrow, a delimiter that never closed — carries
    its own code instead.

