error: `Pair<Int, Int>` has no method `second` [no-such-method]
  --> cmd/method_on_a_generic_type_of_ones_own/main.buri:13:32
   |
13 |   let _ = io.println(ctx, "${p.second()}").ignore();
   |                                ^^^^^^
   |
   = fix: check the spelling, or declare it in `impl<A, B> Pair<A, B> { ... }` in `//cmd/method_on_a_generic_type_of_ones_own/main.buri`, where the type is declared

     Why
     ───

     A method is looked up in exactly one place: the module that declares the
     receiver's type. There is no extension mechanism, so nobody can add a
     method to a type from outside — which is why the fix points a toolchain
     type at its page rather than at an impl block you could not write.

