error: `Str` is not declared in this module [impl-outside-its-module]
 --> cmd/inherent_impl_for_foreign_type/main.buri:8:6
  |
8 | impl Str {
  |      ^^^
  |
  = there is no way to add a method to someone else's type, which is what keeps `x.f()` a single lookup
  = fix: move the `impl` into `Str`'s own module, or write a free function here and call it as one
