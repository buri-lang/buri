// EXPECT: `Measurable` declares no method `extra`
struct Point {
  export x: Int,
}

trait Measurable {
  fn size(self): Int;
}

impl Measurable for Point {
  fn size(self): Int {
    self.x
  }

  fn extra(self): Int {
    self.x
  }
}
