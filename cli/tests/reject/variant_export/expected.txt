error: a variant carries no `export` of its own; an exported enum exports every one of its variants [variant-export]
  --> cmd/variant_export/main.buri:11:3
   |
11 |   export Circle(Float),
   |   ^^^^^^
   |
   = the enum is the unit of visibility, so a variant's payload fields carry none either
   = fix: delete the `export`
