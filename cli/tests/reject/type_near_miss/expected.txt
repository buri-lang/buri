error: there is no type `Poimt` [unresolved-type]
  --> cmd/type_near_miss/main.buri:10:13
   |
10 | fn readX(p: Poimt): Int {
   |             ^^^^^
   |
   = did you mean `Point`?
   = fix: declare it, import it, or correct the spelling

     What to do
     ──────────

     Correct the spelling, or bring the type into scope — a module declares it
     or an import names it, and there is no third way.

     Why
     ───

     Types are nominal throughout. There is no structural fallback and no
     inference from shape, so a misspelling cannot quietly become a different
     type that happens to fit.

