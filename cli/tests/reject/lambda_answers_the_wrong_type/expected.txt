error: expected `Str`, found `Int` [type-mismatch]
  --> cmd/lambda_answers_the_wrong_type/main.buri:10:37
   |
10 |   let f: fn(Int) => Str = fn(_x) => 5;
   |                                     ^ the expected return type is `Str`
   |
   = expected: `Str`
   = actual: `Int`
   = an integer literal defaults to `Int` but takes any integer type, and `Str` is not one
   = fix: produce a `Str` here, or change what surrounds it to accept an `Int`

     What to do
     ──────────

     Convert explicitly. The diagnostic names the conversion: .toI64() is exact
     for every I32, while a narrowing one returns a Result because not every
     value fits.

     A bare numeric literal is reported as Int or Float, the type it takes when
     nothing pins it. It is not held to that: annotate it — let x: F64 = 1.0, or
     let x: U8 = 1 — and it becomes any type of the same kind.

     Why
     ───

     There is no promotion of any kind, in either direction. A language that
     widened silently would make the width of an arithmetic result depend on the
     shape of the expression rather than on what you wrote.

error: expected `Str`, found `Int` [type-mismatch]
  --> cmd/lambda_answers_the_wrong_type/main.buri:22:41
   |
22 |   let hooks = Hooks { onDone: fn(_x) => 5 };
   |                                         ^ the expected return type is `Str`
   |
   = expected: `Str`
   = actual: `Int`
   = an integer literal defaults to `Int` but takes any integer type, and `Str` is not one
   = fix: produce a `Str` here, or change what surrounds it to accept an `Int`
error: expected `Str`, found `Int` [type-mismatch]
  --> cmd/lambda_answers_the_wrong_type/main.buri:33:19
   |
33 |   apply(fn(_x) => 5)
   |                   ^ the expected return type is `Str`
   |
   = expected: `Str`
   = actual: `Int`
   = an integer literal defaults to `Int` but takes any integer type, and `Str` is not one
   = fix: produce a `Str` here, or change what surrounds it to accept an `Int`
