// EXPECT: expected `F64`, found `{integer}`
from "core/effect" import { Alloc, Stdout };
from "core/host" import * as host;

fn mixed(): Float {
  1 + 1.0
}

export fn main(): Result<(), Str> {
  let ctx = context { Alloc: host.alloc, Stdout: host.stdout };
  let _ = ctx.println("${mixed()}");
  .Ok(())
}
