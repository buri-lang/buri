error: this is a test-only module [test-only-import]
  --> cmd/host_testing_import_in_program/main.buri:10:6
   |
10 | from "core/host/testing" import * as testing;
   |      ^^^^^^^^^^^^^^^^^^^ importable only from a test source
   |
   = a path containing a `testing` segment may be imported only from a test source
   = //cmd/host_testing_import_in_program/main.buri is not one
   = fix: import it from a file listed in a target's `test.sources`, or drop the import

     What to do
     ──────────

     Import it from a test source, or drop the import.

     Any module path with a testing *directory* segment is covered:
     core/testing/assert, //lib/ledger/testing, //lib/testing/fakes. A file
     called testing.buri is not one, because that segment is the file's own
     name.

