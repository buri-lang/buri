error: `Point` is missing `y` [missing-field-value]
  --> cmd/struct_literal_missing_a_required_field/main.buri:14:11
   |
14 |   let p = Point { x: 1 };
   |           ^^^^^^^^^^^^^^
   |
   = fix: give `y` a value — only an `Option` field may be left out — or start from an existing one with `Point { ..base, field: value }`

     A field whose declared type is Option<...> is not required: leaving it out
     is writing .None for it, so only the other fields are named here.

