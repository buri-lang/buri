error: `snapshot` takes 5 arguments, but 4 were given [wrong-argument-count]
  --> cmd/call_missing_a_middle_argument/main.buri:31:3
   |
31 |   snapshot(ctx, root, state, themes)
   |   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   |
   = expected: 5
   = actual: 4
   = fix: pass the context: the convention is receiver first, context second, everything else after
error: expected `Str`, found `Node` [type-mismatch]
  --> cmd/call_missing_a_middle_argument/main.buri:31:17
   |
31 |   snapshot(ctx, root, state, themes)
   |                 ^^^^ the parameter type is `Str`
   |
   = expected: `Str`
   = actual: `Node`
   = fix: produce a `Str` here, or change what surrounds it to accept a `Node`

     What to do
     ──────────

     Convert explicitly. The diagnostic names the conversion: .toI64() is exact
     for every I32, while a narrowing one returns a Result because not every
     value fits.

     A bare numeric literal is reported as Int or Float, the type it takes when
     nothing pins it. It is not held to that: annotate it — let x: F64 = 1.0, or
     let x: U8 = 1 — and it becomes any type of the same kind.

     Why
     ───

     There is no promotion of any kind, in either direction. A language that
     widened silently would make the width of an arithmetic result depend on the
     shape of the expression rather than on what you wrote.

error: expected `Node`, found `State` [type-mismatch]
  --> cmd/call_missing_a_middle_argument/main.buri:31:23
   |
31 |   snapshot(ctx, root, state, themes)
   |                       ^^^^^ the parameter type is `Node`
   |
   = expected: `Node`
   = actual: `State`
   = fix: produce a `Node` here, or change what surrounds it to accept a `State`
error: expected `State`, found `[Theme]` [type-mismatch]
  --> cmd/call_missing_a_middle_argument/main.buri:31:30
   |
31 |   snapshot(ctx, root, state, themes)
   |                              ^^^^^^ the parameter type is `State`
   |
   = expected: `State`
   = actual: `[Theme]`
   = fix: produce a `State` here, or change what surrounds it to accept a `[Theme]`
