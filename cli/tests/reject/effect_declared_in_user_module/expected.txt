error: only a platform module may declare an effect [effect-outside-platform]
 --> cmd/effect_declared_in_user_module/main.buri:6:1
  |
6 | effect Mischief {
  | ^^^^^^^^^^^^^^^^^
  |
  = the set of things a Buri program can do to the world is fixed by its platform rather than open-ended
  = fix: declare it as a plain `trait`, or move it into a platform module

    What to do
    ──────────

    Declare it as a plain trait, or move it into a platform module.

    Why
    ───

    The set of things a program can do to the world is fixed by its platform
    rather than open-ended. An effect a library could declare would be authority
    a library could mint.

