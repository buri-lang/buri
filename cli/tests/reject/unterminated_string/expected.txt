error: unterminated string literal [unterminated-string]
 --> cmd/unterminated_string/main.buri:3:8
  |
3 |   "oops
  |        ^
  |
  = fix: close it with `"`; a string literal does not span a line break

    The quote swallows the rest of the line, so this is the only error you get
    for it. A separator, terminator or closing delimiter the parser then misses
    may be sitting inside the string, and it says nothing about a token you may
    well have written.

