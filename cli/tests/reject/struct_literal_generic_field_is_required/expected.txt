error: `Holder` is missing `only` [missing-field-value]
  --> cmd/struct_literal_generic_field_is_required/main.buri:14:32
   |
14 |   let h: Holder<Option<Int>> = Holder { };
   |                                ^^^^^^^^^^
   |
   = fix: give `only` a value — only an `Option` field may be left out — or start from an existing one with `Holder { ..base, field: value }`

     A field whose declared type is Option<...> is not required. Leaving it out
     writes .None for it, so the message names only the other fields.

