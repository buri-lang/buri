error: `Region` is not a declared effect [not-an-effect]
 --> cmd/context_binding_not_an_effect/main.buri:7:46
  |
7 |   let ctx = context { Allocator: host.alloc, Region: host.alloc, Stdout: host.stdout };
  |                                              ^^^^^^
  |
  = fix: name an effect the platform declares, as in `Allocator` or `Stdout`; `Region` is not one

    What to do
    ──────────

    Every key in a context has to be an effect. The set is core/effect's, plus
    ui/effect's where the platform grants them.

    An effect the standard library renamed is answered with what it is called
    now: Alloc was renamed to Allocator, and the fix says to write it.

