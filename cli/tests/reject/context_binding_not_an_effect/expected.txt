error: `Region` is not a declared effect [not-an-effect]
 --> cmd/context_binding_not_an_effect/main.buri:7:42
  |
7 |   let ctx = context { Alloc: host.alloc, Region: host.alloc, Stdout: host.stdout };
  |                                          ^^^^^^
  |
  = fix: name an effect the platform declares, as in `Alloc` or `Stdout`; `Region` is not one

    What to do
    ──────────

    Name an effect the platform declares, as in Alloc or Stdout.

    Why
    ───

    A context binds effects to implementations, so each key has to be one — and
    the set of them is core/effect's, plus ui/effect's where the platform grants
    them.

