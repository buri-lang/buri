error: `Outer` cannot derive `Eq`: `inner` has type `Inner` [underivable]
  --> cmd/derive_unsatisfied_field/main.buri:10:1
   |
10 | derive Eq for Outer;
   | ^^^^^^^^^^^^^^^^^^^^
   |
   = a derived implementation is a fold over the type's components, and `Inner` does not satisfy `Eq`
   = fix: make `Inner` satisfy `Eq` first — `derive Eq for Inner;` in its own module, or an `impl` — or drop `Eq` from this `derive`
error: `Outer` does not implement `Eq` [missing-conformance]
  --> cmd/derive_unsatisfied_field/main.buri:15:26
   |
15 |   let _ = ctx.println("${a == a}");
   |                          ^^^^^^
   |
   = conformance is nominal: a type satisfies a trait only where a declaration says so
   = fix: add `derive Eq for Outer;` in that type's own module, or write `impl Eq for Outer { ... }` there
