error: `Alloc` is bound twice [duplicate-bound]
 --> cmd/context_duplicate_binding/main.buri:6:42
  |
6 |   let ctx = context { Alloc: host.alloc, Alloc: host.alloc, Stdout: host.stdout };
  |                                          ^^^^^^^^^^^^^^^^^
  |
  = a spread's binding is replaced by an explicit one, but two explicit bindings of one effect are a mistake
  = fix: delete one of the two bindings

    What to do
    ──────────

    Delete one of the two bindings.

    Why
    ───

    A spread's binding is replaced by an explicit one — context { ..Hermetic(),
    Fs: files([]) } is how a test overrides a default — but two explicit
    bindings of one effect have no such reading, so the later one is not a
    silent winner.

