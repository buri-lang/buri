error: comparison operators are non-associative [chained-comparison]
 --> cmd/chained_comparison/main.buri:7:9
  |
7 |   a < b < c
  |         ^
  |
  = write `a < b && b < c` rather than `a < b < c`
  = fix: write `a < b && b < c` rather than `a < b < c`

    What to do
    ──────────

    Write a < b && b < c.

    Why
    ───

    Non-associativity is what makes f<T>(x) readable as a call: under it, (f <
    T) > (x) is not a program either, so no source has two readings to disagree
    about.

