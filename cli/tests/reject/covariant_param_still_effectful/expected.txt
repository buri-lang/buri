error: `held` carries an effect, so it must be named `ctx` [effect-param-not-ctx]
  --> cmd/covariant_param_still_effectful/main.buri:16:36
   |
16 | fn peek<C: Alloc + Stdout>(ctx: C, held: Boxed<C>): Int {
   |                                    ^^^^^^^^^^^^^^
   |
   = a function is effectful if and only if it has a `ctx` parameter or an effect-carrying `self`, which is what lets a reader stop after the first two parameters
   = fix: rename `held` to `ctx` and make it the first parameter, or drop the effect bound if this parameter is ordinary data
