error: `main` must return `Result<(), Str>` [main-signature]
 --> cmd/main_wrong_return_type/main.buri:5:19
  |
5 | export fn main(): Int {
  |                   ^^^
  |
  = `.Ok(())` exits 0; `.Err(msg)` prints `msg` to stderr and exits 1
  = fix: change the return type to `Result<(), Str>`

    What to do
    ──────────

    Drop them. main takes no parameters, declares no generic parameters, and
    returns Result<(), Str>.

    Why
    ───

    main is called by the runtime rather than by a program, so there is no call
    site to infer a type argument from and nothing to pass an argument in.

