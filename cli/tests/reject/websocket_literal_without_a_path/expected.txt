error: `WebSocket` is missing `path` [missing-field-value]
  --> cmd/websocket_literal_without_a_path/main.buri:21:22
   |
21 |     websocket: .Some(WebSocket {
   |                      ^^^^^^^^^^^
   |
   = fix: give `path` a value — only an `Option` field may be left out — or start from an existing one with `WebSocket { ..base, field: value }`

     A field whose declared type is Option<...> is not required: leaving it out
     is writing .None for it, so only the other fields are named here.

