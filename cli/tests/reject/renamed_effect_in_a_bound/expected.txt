error: "core/effect" does not export `Alloc` [no-such-export]
 --> cmd/renamed_effect_in_a_bound/main.buri:4:29
  |
4 | from "core/effect" import { Alloc, Stdout };
  |                             ^^^^^
  |
  = `Alloc` was renamed to `Allocator`
  = fix: write `Allocator`

    What to do
    ──────────

    If the module declares the name but holds it back, add export to the
    declaration there. If it declares no such name, the spelling is the mistake.
    The diagnostic says which of the two it is, and where there is a near miss
    the fix names it. A name the standard library renamed is answered outright:
    Alloc was renamed to Allocator.

    Why
    ───

    A re-export may name only what its module path exports, so a library's
    surface is never wider than the modules it is built from.

error: `Alloc` is not a trait or effect [not-a-trait]
 --> cmd/renamed_effect_in_a_bound/main.buri:7:20
  |
7 | export fn greet<C: Alloc + Stdout>(ctx: C): () {
  |                    ^^^^^
  |
  = `Alloc` was renamed to `Allocator`
  = fix: write `Allocator`

    What to do
    ──────────

    Name a declared trait or effect, or declare the one you meant. There are no
    where clauses and no structural constraints.

    A trait or effect the standard library renamed is answered with what it is
    called now: Alloc was renamed to Allocator, and the fix says to write it.

