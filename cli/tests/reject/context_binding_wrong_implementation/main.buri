// EXPECT: does not implement `Alloc`
from "core/effect" import { Alloc, Stdout };
from "core/host" import * as host;

export fn main(): Result<(), Str> {
  let ctx = context { Alloc: host.stdout, Stdout: host.stdout };
  let _ = ctx.println("hi");
  .Ok(())
}
