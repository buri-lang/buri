error: field `0` of `Scope` is private to its module [private-to-module]
  --> cmd/ui_forged_scope/main.buri:14:9
   |
14 |   n.get(Scope(0))
   |         ^^^^^^^^
   |
   = a struct with any private field cannot be constructed or destructured elsewhere, though functional update still works because it never names the hidden fields
   = fix: add `export` to the field, or go through a method `Scope` provides
