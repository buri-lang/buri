error: `State` has no method `show` [no-such-method]
  --> cmd/method_from_a_trait_that_is_not_derived/main.buri:13:29
   |
13 |   let _ = io.println(ctx, s.show(ctx)).ignore();
   |                             ^^^^
   |
   = `show` comes from `Show`, which `State` does not implement; `derive Show for State;` generates it
   = fix: check the spelling, or declare it in `impl State { ... }` in `//cmd/method_from_a_trait_that_is_not_derived/main.buri`, where the type is declared

     Why
     ───

     A method is looked up in exactly one place: the module that declares the
     receiver's type. There is no extension mechanism, so nobody can add a
     method to a type from outside — which is why the fix points a toolchain
     type at its page rather than at an impl block you could not write.

