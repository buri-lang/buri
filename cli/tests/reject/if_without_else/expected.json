{"severity":"error","message":"`if` requires an `else` branch","code":"if-without-else","location":{"file":"cmd/if_without_else/main.buri","line":7,"column":26,"endLine":7,"endColumn":31,"text":"  let label = if (n > 0) { 1 };"},"fix":"add `else { ... }`; an `if` is an expression, so it has a value either way","notes":["`if` is an expression, so both branches must produce a value of the same type"],"related":[]}
