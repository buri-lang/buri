error: field `handle` of `Address` is private to its module [private-to-module]
  --> cmd/actor_address_handle_is_private/main.buri:26:48
   |
26 |     let _ = io.println(ctx, "mailbox ${address.handle}").ignore();
   |                                                ^^^^^^
   |
   = a struct with any private field cannot be constructed or destructured elsewhere, though functional update still works because it never names the hidden fields
   = fix: add `export` to the field, or go through a method `Address` provides

     What to do
     ──────────

     Add export to the field, or go through a method the type provides.

     Why
     ───

     export is the whole of visibility. A declaration, a field or a method
     without it is reachable only from the module that wrote it. One rule, so
     one code: the same error covers a function you cannot call, a field you
     cannot name, and a variant of an enum whose own declaration is private.

     The unusual half is that a struct with any private field cannot be
     constructed anywhere else at all, not merely read: writing Scope(0) names
     the hidden field. That makes a private field an invariant rather than an
     inconvenience, and it is how the standard library mints a type only from
     the inside. ui/effect's Scope is the worked example. The runtime hands a
     reactive closure one, and no program can build one, which is what "a
     closure can never capture a context" rests on.

     Functional update still works, because it never names the hidden fields.

