// EXPECT: carries an effect, so it must be named `ctx`
from "core/effect" import { Alloc, Net, Stdout };
from "core/fs" import { FsRead, Path };
from "core/fs" import * as fs;
from "core/host" import * as host;
from "core/io" import * as io;
from "core/path" import * as filepath;

fn twoWorlds<A: FsRead, B: Net>(ctx: A, other: B, at: Path): Bool {
  fs.exists(ctx, at)
}

export fn main(): Result<(), Str> {
  let ctx = context {
    Alloc: host.alloc,
    FsRead: host.fsRead,
    Net: host.net,
    Stdout: host.stdout,
  };
  let at = filepath.of(ctx, "x");
  let _ = io.println(ctx, "${twoWorlds(ctx, ctx, at)}").ignore();
  .Ok(())
}
