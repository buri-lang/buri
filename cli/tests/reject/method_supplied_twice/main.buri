// EXPECT: `size` is supplied twice
struct Point {
  export x: Int,
}

trait Measurable {
  fn size(self): Int;
}

impl Measurable for Point {
  fn size(self): Int {
    self.x
  }

  fn size(self): Int {
    self.x
  }
}
