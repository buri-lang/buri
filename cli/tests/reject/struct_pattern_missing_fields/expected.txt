error: this pattern does not mention `y` [missing-field-pattern]
 --> cmd/struct_pattern_missing_fields/main.buri:9:7
  |
9 |   let Point { x } = p;
  |       ^^^^^^^^^^^
  |
  = fix: match `y` too, or end the pattern with `..` to ignore the rest

    What to do
    ──────────

    Match y too, or end the pattern with .. to ignore the rest.

    Why
    ───

    Same reason match is exhaustive one level up. Adding a field should be a
    compile error everywhere the type is taken apart, and .. is how a pattern
    opts out.

