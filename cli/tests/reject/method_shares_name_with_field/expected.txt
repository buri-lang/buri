error: `side` is already a field of `Square` [duplicate-field]
 --> cmd/method_shares_name_with_field/main.buri:9:6
  |
9 |   fn side(self): Int {
  |      ^^^^
  |
  = a `.` resolves to a field before a method, so the two may not share a name
  = fix: rename the method, or rename the field

    What to do
    ──────────

    Rename the method, or rename the field.

    Why
    ───

    A . resolves to a field before a method, so sq.side and sq.side() sharing a
    name would be decided by a rule nobody should have to remember.

