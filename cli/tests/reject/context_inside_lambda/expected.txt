error: a context may not be constructed here
 --> cmd/context_inside_lambda/main.buri:8:17
  |
8 |     let inner = context { Alloc: host.alloc, Stdout: host.stdout };
  |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  |
  = only in `main`'s body, in a test source, or in a test-only module — and never inside a lambda, since a closure that could mint authority would make the capture rule meaningless
  = fix: build it in `main` and pass it down as a `ctx` parameter, or make this a test source, where a context may be built per test
