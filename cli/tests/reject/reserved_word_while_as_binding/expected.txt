error: `while` is a reserved word and may not be used as an identifier [reserved-word]
 --> cmd/reserved_word_while_as_binding/main.buri:7:7
  |
7 |   let while = 3;
  |       ^^^^^
  |
  = reserved for a future version of Buri; see grammar.ebnf, ReservedWord
  = fix: pick another name; `while` is not available

    What to do
    ──────────

    Pick another name.

    Why
    ───

    The word is reserved for a future version of the language rather than used
    by this one, so it is refused now instead of becoming a source-breaking
    change later. buri docs grammar lists the whole set under ReservedWord.

error: `while` is a reserved word and may not be used as an identifier [reserved-word]
 --> cmd/reserved_word_while_as_binding/main.buri:8:26
  |
8 |   let _ = ctx.println("${while}");
  |                          ^^^^^
  |
  = reserved for a future version of Buri; see grammar.ebnf, ReservedWord
  = fix: pick another name; `while` is not available
