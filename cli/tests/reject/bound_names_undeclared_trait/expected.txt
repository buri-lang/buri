error: `Bogus` is not a trait or effect [not-a-trait]
 --> cmd/bound_names_undeclared_trait/main.buri:6:15
  |
6 | fn measure<T: Bogus>(x: T): Int { 1 }
  |               ^^^^^
  |
  = a bound names a declared trait; there are no where clauses
  = fix: name a declared trait or effect, or declare `Bogus` as one

    What to do
    ──────────

    Name a declared trait or effect, or declare the one you meant.

    Why
    ───

    A bound resolves to a declaration, and the compiler then checks it with a
    table lookup. There are no where clauses and no structural constraints, so a
    bound has nothing else it could name.

