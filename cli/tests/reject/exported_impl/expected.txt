error: expected a declaration, found `impl` [unexpected-token]
 --> cmd/exported_impl/main.buri:8:8
  |
8 | export impl Eq for Version {
  |        ^^^^
  |
  = expected: a declaration
  = actual: `impl`
  = fix: start it with one of `from` `export` `fn` `struct` `enum` `type` `let` `trait` `effect` `impl` `derive` `context` `test` — a module is a list of declarations, with no statements between them

    What to do
    ──────────

    Write what the message names. This is the parser's catch-all, so the useful
    half of it is always the "expected" part.

