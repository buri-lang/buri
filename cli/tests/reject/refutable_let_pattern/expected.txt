error: this pattern does not match every value of its type [refutable-pattern]
 --> cmd/refutable_let_pattern/main.buri:7:7
  |
7 |   let .Some(n) = o;
  |       ^^^^^^^^
  |
  = a `let` binds unconditionally, so its pattern has to be irrefutable
  = fix: use `match`, which makes you say what the other cases do

    What to do
    ──────────

    Use match. There is no exception to throw when the value does not fit, so
    the other cases have to be written out.

