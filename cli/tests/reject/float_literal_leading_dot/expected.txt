error: expected an identifier, found `5` [unexpected-token]
 --> cmd/float_literal_leading_dot/main.buri:8:15
  |
8 |   let half = .5;
  |               ^
  |
  = expected: an identifier
  = actual: `5`
  = fix: name it: a lowerCamelCase binding, or an UpperCamelCase type

    What to do
    ──────────

    Write what the message names. This is the parser's catch-all, so the useful
    half of it is always the "expected" part.

    A mistake the parser can name — a separator, a terminator, an arrow, a
    delimiter that never closed — carries its own code and says which construct
    wanted it. This one is what is left: a token that begins nothing the grammar
    admits in a position where several things would have been admissible.

error: there is nothing named `half` in scope [unresolved-name]
 --> cmd/float_literal_leading_dot/main.buri:9:30
  |
9 |   let _ = io.println(ctx, "${half}").ignore();
  |                              ^^^^
  |
  = fix: declare `half`, or import it — a name is in scope only from this module's own declarations and its imports

    What to do
    ──────────

    Correct the spelling, or declare it. The diagnostic offers the nearest name
    in scope.

    Why
    ───

    There is no prelude and no ambient scope: the names available in a module
    are the ones it declares plus the ones its own imports name. That is what
    makes the suggestion trustworthy — the set it is drawn from is exactly the
    set the file put there.

