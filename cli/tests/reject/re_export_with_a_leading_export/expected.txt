error: write `from "..." export { ... }`, without a leading `export` [re-export-with-a-leading-export]
 --> cmd/re_export_with_a_leading_export/main.buri:2:8
  |
2 | export from "core/list" export { map };
  |        ^^^^
  |
  = fix: drop the leading `export`: the `export` after the path is the one that re-exports
