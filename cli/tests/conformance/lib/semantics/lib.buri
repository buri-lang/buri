// The public surface of //lib/semantics.

from "//lib/semantics/shapes.buri" export {
    anonHere, anonHidden, anonMatch, applyTwice, area, Backwards, between, Boxed, Brick,
    callWith, Cell, cellIndex, compose, composeThrice, constant, Count, declIndex,
    defaulted, defaultedResult, deferred, Delta, depth, eachRow, EchoEnv, Either,
    eitherInt, elapsed, Feather, fetchBody, flip, fourStrs, FrozenClock, greet, Guarded,
    guarded, has, heaviest, heavyLabel, Held, heldTwice, Hidden, hidden, hiddenPrivate,
    Holder, hypotenuse, identity, inOrder, insert, joinedWith, Label, lambdaTry,
    lambdaTryResult, largest, Level, loadText, Logged, loggedWith, Loose, makeAdder,
    makeScaler, mapTree, Meters, nestedShadow, note, noteBool, noteIf, noteInt,
    noteLets, noteLevel, noteMatch, noteOpt, noteRes, noteStr, occursIn, Offset,
    OneShotListen, Pair, pair, parseSum, Percent, pipeline, PlanListen, PoolListen,
    pushedWith, QuietSockets, rank, ReadOnly, Relay, relayAll, relayScaled, relayTwice,
    render, repeatedWith, Report, Rose, roseSum, roundTrip, runInOrder, runInOrderNamed,
    saveText, Scale, SerialTasks, serveOnce, shadowed, Shape, shout, showAll, size,
    Slot, slotOr, smallest, sortedShow, stamp, stampedWith, StaticFs, Suit, sumFirstTwo,
    swap, takesWorld, TeapotNet, threeInts, Ticking, ticking, totalGrams, Tree, treeOf,
    triple, twice, Version, version, Weigh, weighAndShow, Widget, World, worldHere, Wrap,
};
// derived traits
// newtypes with derived operator traits
// hand-written impls
// traits of my own
// a trait method whose written parameter type is `Self`
// generic functions
// generic structs and enums
// closures
// `?` and defaults
// blocks and scope
// effect users
// fakes
// which positions of a type constructor hand a value back
// fields a literal may leave out
// literals that leave the type name out too
// evaluation-order instrumentation
