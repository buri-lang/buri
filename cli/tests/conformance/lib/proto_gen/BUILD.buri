library {
    sources: ["schemas.buri"]

    test {
        sources: [
            "test/mapping.buri",
            "test/codecs.buri",
            "test/origins.buri",
            "test/failures.buri",
            "test/entry.buri",
        ]

        # The reference run, on the reference backend: `native::conformance`
        # drives these same files through the stencil backend
        # (cli/tests/README.md).
        platforms: [JS]
    }
}
