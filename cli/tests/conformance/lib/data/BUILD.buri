library {
    sources: []

    test {
        sources: [
            "test/lists.buri",
            "test/strings.buri",
            "test/patterns.buri",
            "test/optionresult.buri",
        ]

        # The reference run, on the reference backend: `native::conformance`
        # drives these same files through Cranelift (cli/tests/README.md).
        platforms: [JS]
    }
}
