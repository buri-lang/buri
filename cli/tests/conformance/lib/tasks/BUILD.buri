library {
    sources: []

    test {
        sources: ["test/background.buri", "test/scope.buri"]

        # The reference run, on the reference backend: `native::conformance`
        # drives this same file through the copy-and-patch backend
        # (cli/tests/README.md).
        platforms: [JS]
    }
}
