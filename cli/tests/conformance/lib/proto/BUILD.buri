library {
    proto_sources: ["address.proto", "demo.proto"]

    visibility: ["//visibility:public"]

    test {
        sources: ["test/binary.buri", "test/failures.buri", "test/json.buri"]

        # The reference run, on the reference backend: `native::conformance`
        # drives these same files through the stencil backend
        # (cli/tests/README.md).
        platforms: [JS]
    }
}
