// `format` is on the surface, so it resolves from outside this library. That
// is what makes the case possible: //cmd/app can call it without naming
// //lib/money in an import.

from "//lib/money/cents" export { Cents, fromCents, format };
