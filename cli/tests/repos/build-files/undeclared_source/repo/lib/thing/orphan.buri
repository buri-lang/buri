// On disk, in the package, and listed by no rule. Nothing compiles it and
// nothing says so, which is the state `undeclared-source` exists to end.

// Not exported: once the rule lists this file the package compiles it, and an
// `export` nothing reaches would be a second finding on top of the one this
// case is about.
fn orphaned(): Str { "nobody builds me" }
