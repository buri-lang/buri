cli/src/docs/build/build-files.md:378  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/libraries.md:171  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/libraries.md:215  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/libraries.md:23  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/libraries.md:230  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/libraries.md:30  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/libraries.md:323  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/libraries.md:46  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/overview.md:102  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/overview.md:116  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/overview.md:133  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:138  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:180  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:198  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:217  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:238  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:249  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:281  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:307  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:342  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/build/testing.md:80  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/guide/methods-and-traits.md:19  names a module in another repository, so it cannot be compiled standalone; the same pattern is compiled in cli/tests/example
cli/src/docs/guide/three-ideas.md:53  signatures of `core/list`; a method may only be declared in its type's defining module, so these compile in cli/src/std/list.buri and are rendered from it by `buri docs std core/list`
cli/src/docs/lang/effects.md:105  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:11  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:130  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:179  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:195  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:217  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:223  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:235  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:257  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:291  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:44  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/effects.md:61  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/evaluation.md:10  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/evaluation.md:37  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:137  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:159  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:167  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:179  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:188  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:204  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:224  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:244  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:261  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:292  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:307  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:315  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:330  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:395  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:416  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/expressions.md:71  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/functions.md:4  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/lexical.md:62  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/lexical.md:89  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/modules.md:102  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/programs.md:156  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/programs.md:173  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/programs.md:194  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/programs.md:220  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/programs.md:232  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/programs.md:255  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/programs.md:6  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:106  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:138  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:172  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:204  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:234  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:257  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:265  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:291  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:316  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:326  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:342  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:366  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:443  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:466  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:49  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:492  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:65  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:81  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
cli/src/docs/lang/types.md:94  not yet converted to a compiled example: it references names the document never declares, so it needs a preamble before the harness can check it
