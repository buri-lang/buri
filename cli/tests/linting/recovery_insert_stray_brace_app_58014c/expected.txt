warning: this discards a `Result` [discarded-result]
  --> lib/recovery_insert_stray_brace_app_58014c/main.buri:26:13
   |
26 |     let _ = record().ignore();
   |             ^^^^^^^^^^^^^^^^^
   |
   = `ignore` is the one way to drop a `Result`, so every place a failure is deliberately unhandled is one of these
   = fix: handle the error with `match`, propagate it with `?`, or keep `ignore` if dropping it is deliberate
error: an expression statement is legal only in a test source [expression-statement]
  --> lib/recovery_insert_stray_brace_app_58014c/main.buri:27:5
   |
27 |     extra let _ = io.println(ctx, "done").ignore();
   |     ^^^^^
   |
   = a block is `let`s followed by a result expression
   = fix: bind it: `let _ = ...;`, or make it the block's result expression
error: there is nothing named `extra` in scope [unresolved-name]
  --> lib/recovery_insert_stray_brace_app_58014c/main.buri:27:5
   |
27 |     extra let _ = io.println(ctx, "done").ignore();
   |     ^^^^^
   |
   = fix: declare `extra`, or import it — a name is in scope only from this module's own declarations and its imports
error: a statement ends with `;` [missing-terminator]
  --> lib/recovery_insert_stray_brace_app_58014c/main.buri:27:10
   |
27 |     extra let _ = io.println(ctx, "done").ignore();
   |          ^
   |
   = fix: write `;` here
warning: this discards a `Result` [discarded-result]
  --> lib/recovery_insert_stray_brace_app_58014c/main.buri:27:19
   |
27 |     extra let _ = io.println(ctx, "done").ignore();
   |                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   |
   = `ignore` is the one way to drop a `Result`, so every place a failure is deliberately unhandled is one of these
   = fix: handle the error with `match`, propagate it with `?`, or keep `ignore` if dropping it is deliberate
