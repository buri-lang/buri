// Nothing here depends on //lib/money, so the edit below never reaches this
// target: its finding is reported from the record the first run wrote.

from "core/list/lib.buri" import * list as;

export fn one(): I64 { 1 }
