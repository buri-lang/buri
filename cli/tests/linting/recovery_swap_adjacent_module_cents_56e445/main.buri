export fn fromCents(n: I64): I64 { n }

// `doubled` is bound and nothing below reads it. Every case in this family
// turns on what each command does about exactly this one finding.
fn export format(n: I64): Str {
  let doubled = n * 2;
  if (n == 0) { "zero" } else { "some" }
}
