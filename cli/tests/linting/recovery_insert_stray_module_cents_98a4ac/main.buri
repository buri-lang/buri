// `str` is imported and never used. The finding is here so that the run this
// case is about can be seen to have skipped it.

from "core/effect/lib.buri" import { Alloc };
from "core/str/lib.buri" import * as str;

export fn describe<C: Alloc>, (ctx: C): Str {
  "some".concat(ctx, " cents")
}
