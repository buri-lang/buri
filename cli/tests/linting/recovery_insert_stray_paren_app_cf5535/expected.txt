warning: this is a table of the sixteen hexadecimal digits [hex-digit-table]
  --> lib/recovery_insert_stray_paren_app_cf5535/main.buri:12:5
   |
12 |     '0',
   |     ^^^
   |
   = `char.fromDigit(n, 16)` is a digit and `char.toDigit(16)` reads one back, so a table of them is a copy of something the standard library keeps
   = fix: delete the table, and reach for `char.fromDigit`, `num.toHex` or `bytes.toHex`
error: `nibble` takes 2 arguments, but 1 were given [wrong-argument-count]
  --> lib/recovery_insert_stray_paren_app_cf5535/main.buri:40:44
   |
40 |     str.fromChars(ctx, [nibble(octet, 16), nibble(octet@ , 1)])
   |                                            ^^^^^^^^^^^^^^^^^^
   |
   = expected: 2
   = actual: 1
   = fix: pass exactly 2
error: this call is missing its closing `)` [unclosed-delimiter]
  --> lib/recovery_insert_stray_paren_app_cf5535/main.buri:40:56
   |
40 |     str.fromChars(ctx, [nibble(octet, 16), nibble(octet@ , 1)])
   |                                                        ^
   |
  --> lib/recovery_insert_stray_paren_app_cf5535/main.buri:40:50
   |
40 |     str.fromChars(ctx, [nibble(octet, 16), nibble(octet@ , 1)])
   |                                                  ^ opened here
   |
   = fix: write `)` here
