warning: this branch is nested 5 levels deep [deep-nesting]
  --> lib/recovery_swap_adjacent_brace_app_ed6367/main.buri:14:21
   |
14 |                     if (n > 10000) { "huge" } else { "large" }
   |                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   |
   = the limit is 4; an `else if` chain counts as one level, and a lambda body starts again at zero
   = fix: pull the inner branch into a named function, or replace the ladder with one `match`
error: `if` requires an `else` branch [if-without-else]
  --> lib/recovery_swap_adjacent_brace_app_ed6367/main.buri:30:17
   |
30 |     if (n == 0) {
   |                 ^
   |
   = `if` is an expression, so both branches must produce a value of the same type
   = fix: add `else { ... }`; an `if` is an expression, so it has a value either way
