error: `if` requires an `else` branch [if-without-else]
 --> lib/recovery_delete_closer_brace_cents_12ff3d/lib.buri:7:15
  |
7 |   if (n == 0) { "zero"  else { "some" }
  |               ^^^^^^^^^^^^^^^^^^^^^^^^^
  |
  = `if` is an expression, so both branches must produce a value of the same type
  = fix: add `else { ... }`; an `if` is an expression, so it has a value either way
error: this `if` branch is missing its closing `}` [unclosed-delimiter]
 --> lib/recovery_delete_closer_brace_cents_12ff3d/lib.buri:7:25
  |
7 |   if (n == 0) { "zero"  else { "some" }
  |                         ^^^^
  |
 --> lib/recovery_delete_closer_brace_cents_12ff3d/lib.buri:7:15
  |
7 |   if (n == 0) { "zero"  else { "some" }
  |               ^ opened here
  |
  = fix: write `}` here
error: this function body is missing its closing `}` [unclosed-delimiter]
 --> lib/recovery_delete_closer_brace_cents_12ff3d/lib.buri:9:1
  |
9 | 
  | ^
  |
 --> lib/recovery_delete_closer_brace_cents_12ff3d/lib.buri:5:31
  |
5 | export fn format(n: I64): Str {
  |                               ^ opened here
  |
  = fix: write `}` here
