warning: `size` is 41 lines long [oversized-function]
 --> lib/recovery_delete_closer_brace_app_bf8f84/main.buri:9:4
  |
9 | fn size(n: I64): Str {
  |    ^^^^
  |
  = a body past 40 lines is almost always carrying more than one responsibility, and the length is the symptom rather than the fault
  = fix: find the responsibility boundaries in the body and give each one a function of its own
error: `if` requires an `else` branch [if-without-else]
  --> lib/recovery_delete_closer_brace_app_bf8f84/main.buri:10:16
   |
10 |     if (n > 0) {
   |                ^
   |
   = `if` is an expression, so both branches must produce a value of the same type
   = fix: add `else { ... }`; an `if` is an expression, so it has a value either way
error: this `if` branch is missing its closing `}` [unclosed-delimiter]
  --> lib/recovery_delete_closer_brace_app_bf8f84/main.buri:14:46
   |
14 |                     if (n > 10000) { "huge"  else { "large" }
   |                                              ^^^^
   |
  --> lib/recovery_delete_closer_brace_app_bf8f84/main.buri:14:36
   |
14 |                     if (n > 10000) { "huge"  else { "large" }
   |                                    ^ opened here
   |
   = fix: write `}` here
error: this function body is missing its closing `}` [unclosed-delimiter]
  --> lib/recovery_delete_closer_brace_app_bf8f84/main.buri:50:1
   |
50 | 
   | ^
   |
 --> lib/recovery_delete_closer_brace_app_bf8f84/main.buri:9:22
  |
9 | fn size(n: I64): Str {
  |                      ^ opened here
  |
  = fix: write `}` here
