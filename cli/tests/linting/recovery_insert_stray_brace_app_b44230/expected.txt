warning: `ctx` is not used; the function is pure [unused-context]
  --> lib/recovery_insert_stray_brace_app_b44230/main.buri:15:20
   |
15 | fn width<C: Alloc>(ctx: C, label: Str): Int {
   |                    ^^^^^^
   |
   = a `ctx` parameter is the whole of what a function may do to the world, so one the body never reads makes every caller hold a context for a computation that touches nothing
   = fix: remove the `ctx` parameter, and the context argument at every call site
error: expected `;`, found `@` [unexpected-token]
  --> lib/recovery_insert_stray_brace_app_b44230/main.buri:37:23
   |
37 |   let label = "ledger"@ ;
   |                       ^
   |
   = expected: `;`
   = actual: `@`
   = fix: write `;` here
error: there is nothing named `label` in scope [unresolved-name]
  --> lib/recovery_insert_stray_brace_app_b44230/main.buri:38:25
   |
38 |   let _ = announce(ctx, label);
   |                         ^^^^^
   |
   = fix: declare `label`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `label` in scope [unresolved-name]
  --> lib/recovery_insert_stray_brace_app_b44230/main.buri:39:38
   |
39 |   let _ = ctx.println("${banner(ctx, label)} is ${width(ctx, label)} wide");
   |                                      ^^^^^
   |
   = fix: declare `label`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `label` in scope [unresolved-name]
  --> lib/recovery_insert_stray_brace_app_b44230/main.buri:39:62
   |
39 |   let _ = ctx.println("${banner(ctx, label)} is ${width(ctx, label)} wide");
   |                                                              ^^^^^
   |
   = fix: declare `label`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `label` in scope [unresolved-name]
  --> lib/recovery_insert_stray_brace_app_b44230/main.buri:40:74
   |
40 |   let _ = ctx.println(rendered(ctx, fn(c, l) => str.format(c, "<${l}>"), label));
   |                                                                          ^^^^^
   |
   = fix: declare `label`, or import it — a name is in scope only from this module's own declarations and its imports
