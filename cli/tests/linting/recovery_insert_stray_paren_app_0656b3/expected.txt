warning: this branch is nested 5 levels deep [deep-nesting]
  --> lib/recovery_insert_stray_paren_app_0656b3/main.buri:14:11
   |
14 |           if (extra n > 10000) { "huge" } else { "large" }
   |           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   |
   = the limit is 4; an `else if` chain counts as one level, and a lambda body starts again at zero
   = fix: pull the inner branch into a named function, or replace the ladder with one `match`
error: there is nothing named `extra` in scope [unresolved-name]
  --> lib/recovery_insert_stray_paren_app_0656b3/main.buri:14:15
   |
14 |           if (extra n > 10000) { "huge" } else { "large" }
   |               ^^^^^
   |
   = fix: declare `extra`, or import it — a name is in scope only from this module's own declarations and its imports
error: this `if` condition is missing its closing `)` [unclosed-delimiter]
  --> lib/recovery_insert_stray_paren_app_0656b3/main.buri:14:21
   |
14 |           if (extra n > 10000) { "huge" } else { "large" }
   |                     ^
   |
  --> lib/recovery_insert_stray_paren_app_0656b3/main.buri:14:14
   |
14 |           if (extra n > 10000) { "huge" } else { "large" }
   |              ^ opened here
   |
   = fix: write `)` here
