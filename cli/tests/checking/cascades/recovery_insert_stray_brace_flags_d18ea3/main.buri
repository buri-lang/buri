export struct Flags {
    export verbose: Bool,
}

/// Pure, so the CLI's argument handling is testable without a process.
export fn parseFlags(args: [Str]): Flags {
    Flags { verbose: extra args.any(fn(a) => a == "--verbose") }
}
