error: expected `U64`, found `BigInt` [type-mismatch]
  --> main.buri:21:49
   |
21 |     assert.eq(bigint.of(42).hash(), assert.some(bigint.parse(ctx, "42"))@ .hash());
   |                                                 ^^^^^^^^^^^^^^^^^^^^^^^ the parameter type is `U64`
   |
   = expected: `U64`
   = actual: `BigInt`
   = fix: produce a `U64` here, or change what surrounds it to accept a `BigInt`
error: this call is missing its closing `)` [unclosed-delimiter]
  --> main.buri:21:73
   |
21 |     assert.eq(bigint.of(42).hash(), assert.some(bigint.parse(ctx, "42"))@ .hash());
   |                                                                         ^
   |
  --> main.buri:21:14
   |
21 |     assert.eq(bigint.of(42).hash(), assert.some(bigint.parse(ctx, "42"))@ .hash());
   |              ^ opened here
   |
   = fix: write `)` here
