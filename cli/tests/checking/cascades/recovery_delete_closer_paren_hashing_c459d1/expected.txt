error: expected `U64`, found `BigInt` [type-mismatch]
  --> main.buri:38:42
   |
38 |     assert.notEqual(bigint.of(1).hash(), bigint.of(2.hash());
   |                                          ^^^^^^^^^^^^^^^^^^^ the parameter type is `U64`
   |
   = expected: `U64`
   = actual: `BigInt`
error: expected `I64`, found `U64` [type-mismatch]
  --> main.buri:38:52
   |
38 |     assert.notEqual(bigint.of(1).hash(), bigint.of(2.hash());
   |                                                    ^^^^^^^^ the parameter type is `I64`
   |
   = expected: `I64`
   = actual: `U64`
   = there is no implicit promotion of any kind
   = fix: convert explicitly with `.toI64()?`, which returns a `Result<I64, RangeError>` because not every `U64` fits
error: this call is missing its closing `)` [unclosed-delimiter]
  --> main.buri:38:61
   |
38 |     assert.notEqual(bigint.of(1).hash(), bigint.of(2.hash());
   |                                                             ^
   |
  --> main.buri:38:20
   |
38 |     assert.notEqual(bigint.of(1).hash(), bigint.of(2.hash());
   |                    ^ opened here
   |
   = fix: write `)` here
