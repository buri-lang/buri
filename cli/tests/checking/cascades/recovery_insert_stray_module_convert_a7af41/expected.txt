error: expected `;`, found `@` [unexpected-token]
  --> main.buri:16:46
   |
16 | from "core/testing/assert" import * as assert@ ;
   |                                              ^
   |
   = expected: `;`
   = actual: `@`
   = fix: write `;` here
error: there is nothing named `assert` in scope [unresolved-name]
  --> main.buri:19:5
   |
19 |     assert.equal(simd.f32x4(1.9, -1.9, 0.0, 7.0).toInt(), simd.i32x4(1, -1, 0, 7));
   |     ^^^^^^
   |
   = fix: declare `assert`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `assert` in scope [unresolved-name]
  --> main.buri:20:5
   |
20 |     assert.equal(simd.f32x4(-0.5, 0.5, -2.5, 2.5).toInt(), simd.i32x4(0, 0, -2, 2));
   |     ^^^^^^
   |
   = fix: declare `assert`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `assert` in scope [unresolved-name]
  --> main.buri:21:5
   |
21 |     assert.equal(simd.ZERO_FLOAT.toInt(), simd.ZERO_INT);
   |     ^^^^^^
   |
   = fix: declare `assert`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `assert` in scope [unresolved-name]
  --> main.buri:28:5
   |
28 |     assert.equal(got.lane(0), 0);
   |     ^^^^^^
   |
   = fix: declare `assert`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `assert` in scope [unresolved-name]
  --> main.buri:29:5
   |
29 |     assert.equal(got.lane(1), number.maxValue<I64>());
   |     ^^^^^^
   |
   = fix: declare `assert`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `assert` in scope [unresolved-name]
  --> main.buri:30:5
   |
30 |     assert.equal(got.lane(2), number.minValue<I64>());
   |     ^^^^^^
   |
   = fix: declare `assert`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `assert` in scope [unresolved-name]
  --> main.buri:31:5
   |
31 |     assert.equal(got.lane(3), 3);
   |     ^^^^^^
   |
   = fix: declare `assert`, or import it — a name is in scope only from this module's own declarations and its imports
error: there is nothing named `assert` in scope [unresolved-name]
  --> main.buri:36:5
   |
36 |     assert.equal(v.toFloat().toInt(), v);
   |     ^^^^^^
   |
   = fix: declare `assert`, or import it — a name is in scope only from this module's own declarations and its imports
