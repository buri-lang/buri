error: "//lib/units/units.buri" does not export `Raw` [no-such-export]
 --> lib/units/lib.buri:5:20
  |
5 |     depth, Meters, Raw, Sample, sample, Trip, widen,
  |                    ^^^
  |
  = a re-export may name only what its module path exports
  = `Raw` is declared in "//lib/units/units.buri" but not exported
  = fix: add `export` to `Raw`'s declaration in "//lib/units/units.buri", or drop it from this list

    Why
    ───

    A re-export may name only what its module path exports, so a library's
    surface is never wider than the modules it is built from.

