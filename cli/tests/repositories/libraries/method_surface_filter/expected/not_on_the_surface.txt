error: `toCents` is not on //lib/money's surface [not-on-the-surface]
  --> cmd/app/main.buri:12:37
   |
12 |   io.println(ctx, "${fromCents(450).toCents()}").mapErr(fn(_e) => "could not write to standard output")
   |                                     ^^^^^^^
   |
   = a method call from outside a library resolves only to names its lib.buri exports
   = fix: re-export `toCents` from that library's lib.buri, if it is part of the API
