# A library that ships fakes for other people's tests, in a `testing/`
# subdirectory with its own surface.

library {
  sources: ["w.buri"]
  visibility: ["//visibility:public"]

  testing {
    sources: ["testing/fixtures.buri"]
  }
}
