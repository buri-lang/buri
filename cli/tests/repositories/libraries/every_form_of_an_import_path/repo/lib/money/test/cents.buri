from "core/testing/assert" import * as assert;
from "//lib/money" import { Cents };
from "//lib/money/testing" import { sample };

test "the testing surface builds a Cents" {
    assert.equal(sample().0, 1905);
}
