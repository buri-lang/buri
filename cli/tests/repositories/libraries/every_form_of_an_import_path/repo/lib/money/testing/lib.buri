//! The testing surface: a file two directories down that an import names in
//! full, where it used to be spelled "//lib/money/testing".

from "//lib/money/lib.buri" import { Cents, fromCents };

/// A round figure, for a suite that needs one and does not care which.
export fn sample(): Cents { fromCents(1905) }
