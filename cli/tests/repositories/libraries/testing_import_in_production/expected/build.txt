error: this is a test-only module [test-only-import]
 --> cmd/app/main.buri:8:6
  |
8 | from "//lib/money/testing" import { sample };
  |      ^^^^^^^^^^^^^^^^^^^^^ importable only from a test source
  |
  = a path containing a `testing` segment may be imported only from a test source
  = //cmd/app/main.buri is not one
  = fix: import it from a file listed in a target's `test.sources`, or drop the import

    What to do
    ──────────

    Import it from a file listed in a target's test.sources, or drop the import.

    Why
    ───

    The path carries the restriction rather than a field, so you can see it
    where the import is written and there is nothing to remember to declare. Any
    module path with a testing *directory* segment is covered:
    core/testing/assert, //lib/ledger/testing, //lib/testing/fakes. A file
    called testing.buri is not one, because the segment that would have made it
    test-only is the file's own name.

error: there is nothing named `sample` in scope [unresolved-name]
  --> cmd/app/main.buri:16:23
   |
16 |         .println(ctx, sample().format(ctx))
   |                       ^^^^^^
   |
   = fix: declare `sample`, or import it — a name is in scope only from this module's own declarations and its imports

     What to do
     ──────────

     Correct the spelling, or declare it. The diagnostic offers the nearest name
     in scope.

     Why
     ───

     There is no prelude and no ambient scope. A module's available names are
     the ones it declares plus the ones its own imports name. That is what makes
     the suggestion trustworthy: it is drawn from exactly the set the file put
     there.

