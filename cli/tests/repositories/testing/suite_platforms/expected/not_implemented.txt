error: no {{CROSS_PLATFORM}} test run on this toolchain [platform-not-implemented]
 --> lib/codec/BUILD.buri:7:10
  |
7 |     test {
  |          ^
  |
  = a test suite runs where its artifact runs, and this toolchain executes a native suite on its own host only — a cross artifact it can build, it cannot run, so this suite can be executed only on JavaScript
  = fix: drop {{CROSS_PLATFORM_PROTO}} from `test.platforms`, or run this suite where a {{CROSS_PLATFORM}} artifact can be built
