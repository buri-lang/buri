error: //lib/codec cannot be built for {{CROSS_PLATFORM}} [platform-violation]
 --> lib/codec/BUILD.buri:1:1
  |
1 | # `browser` limits this library to JS, and the suite says nothing about tags at
  | ^
  |
  = //lib/codec is tagged "browser", which requires js
  = "browser": runs in a web page, and nowhere else
  = fix: drop the {{CROSS_PLATFORM}} output, or widen the tag's `requires { platforms }` in REPO.buri
