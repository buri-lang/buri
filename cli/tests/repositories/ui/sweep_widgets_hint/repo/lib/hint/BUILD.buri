# One library and one test source: the hint a `field` draws inside its own
# empty box, and a snapshot per axis of it.
library {
    sources: ["scenes.buri"]

    test {
        sources: ["test/pictures.buri"]
    }
}
