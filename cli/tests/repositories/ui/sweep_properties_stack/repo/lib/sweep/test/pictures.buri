// One picture per family of `ui/style` properties, over a `stack`: arrangement
// and nothing else, over two boxes of text.
//
// Resting state throughout: no style here is inside an `On`, so the
// state a snapshot is taken in contributes nothing to any of them.

from "core/effect" import { Allocator };
from "core/host/testing" import { alloc };
from "ui/effect" import { Ui };
from "ui/testing" import { headless, snapshot };
from "//lib/sweep" import {
    gapPage, paddingPage, runsPage, sizePage, surfacePage, typePage,
};

test "the surface properties, on a stack" {
    let ctx = context {
        Allocator: alloc(),
        Ui: headless(),
    };
    snapshot(ctx, "stack-surface", surfacePage(), .Hover, []);
}

test "the type properties, on a stack" {
    let ctx = context {
        Allocator: alloc(),
        Ui: headless(),
    };
    snapshot(ctx, "stack-type", typePage(), .Hover, []);
}

test "how a run of text is set, on a stack" {
    let ctx = context {
        Allocator: alloc(),
        Ui: headless(),
    };
    snapshot(ctx, "stack-runs", runsPage(), .Hover, []);
}

test "the paddings, on a stack" {
    let ctx = context {
        Allocator: alloc(),
        Ui: headless(),
    };
    snapshot(ctx, "stack-padding", paddingPage(), .Hover, []);
}

test "the gaps, and the mark beside a child, on a stack" {
    let ctx = context {
        Allocator: alloc(),
        Ui: headless(),
    };
    snapshot(ctx, "stack-gap", gapPage(), .Hover, []);
}

test "the sizing properties, on a stack" {
    let ctx = context {
        Allocator: alloc(),
        Ui: headless(),
    };
    snapshot(ctx, "stack-size", sizePage(), .Hover, []);
}
