# One library and one test source: six pictures of a `stack`: arrangement and
# nothing else, over two boxes of text, and a snapshot each.
library {
    sources: ["scenes.buri"]

    test {
        sources: ["test/pictures.buri"]
    }
}
