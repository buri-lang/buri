from "ui/node" import * as ui;
from "ui/node" import { Node };

/// The page every picture is drawn on: eight hundred wide with nothing inset
/// either side, so a boundary in a picture is a number off the viewport's own
/// edge.
fn page<C>(rows: [Node<C>]): Node<C> {
    ui.column([.PaddingY(.Px(6)), .Gap(.Px(8)), .FontSize(.Px(13))], rows)
}

/// One row of a picture: the value it shows, and the boxes that show it.
fn band<C>(label: Str, demo: Node<C>): Node<C> {
    ui.column([.Gap(.Px(2))], [
        ui.stack([.FontSize(.Px(11)), .Foreground(.Rgb(90, 90, 100))], [
            ui.text(.Const(label)),
        ]),
        demo,
    ])
}

/// Every `Track` there is, across the whole eight hundred: exactly this
/// wide, a share of what is left, and as wide as the content needs.
export fn tracks<C>(): Node<C> {
    page([
        band(
            "Fixed(.Px(160)) three times",
            ui.stack(
                [
                    .Layout(
                        .Grid([.Fixed(.Px(160)), .Fixed(.Px(160)), .Fixed(.Px(160))]),
                    ),
                ],
                [
                    ui.stack([.Background(.Rgb(200, 40, 40)), .Height(.Px(36))], []),
                    ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(36))], []),
                    ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(36))], []),
                ],
            ),
        ),
        band(
            "Fraction(1) three times",
            ui.stack([.Layout(.Grid([.Fraction(1), .Fraction(1), .Fraction(1)]))], [
                ui.stack([.Background(.Rgb(200, 40, 40)), .Height(.Px(36))], []),
                ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(36))], []),
                ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(36))], []),
            ]),
        ),
        band(
            "Auto three times",
            ui.stack([.Layout(.Grid([.Auto, .Auto, .Auto]))], [
                ui.stack(
                    [
                        .Background(.Rgb(200, 40, 40)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("narrow"))],
                ),
                ui.stack(
                    [
                        .Background(.Rgb(40, 150, 40)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("a middling cell"))],
                ),
                ui.stack(
                    [
                        .Background(.Rgb(40, 40, 200)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("the widest cell of the three"))],
                ),
            ]),
        ),
    ])
}

/// The last row is the one to read with a ruler: an eighty-pixel track,
/// then one share against two, so the boundaries land at 80 and 320 of 800.
export fn fractions<C>(): Node<C> {
    page([
        band(
            "Fraction(1), Fraction(2)",
            ui.stack([.Layout(.Grid([.Fraction(1), .Fraction(2)]))], [
                ui.stack([.Background(.Rgb(200, 40, 40)), .Height(.Px(36))], []),
                ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(36))], []),
            ]),
        ),
        band(
            "Fraction(2), Fraction(1)",
            ui.stack([.Layout(.Grid([.Fraction(2), .Fraction(1)]))], [
                ui.stack([.Background(.Rgb(200, 40, 40)), .Height(.Px(36))], []),
                ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(36))], []),
            ]),
        ),
        band(
            "Fraction(1) three times",
            ui.stack([.Layout(.Grid([.Fraction(1), .Fraction(1), .Fraction(1)]))], [
                ui.stack([.Background(.Rgb(200, 40, 40)), .Height(.Px(36))], []),
                ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(36))], []),
                ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(36))], []),
            ]),
        ),
        band(
            "Fixed(.Px(80)), Fraction(1), Fraction(2)",
            ui.stack([.Layout(.Grid([.Fixed(.Px(80)), .Fraction(1), .Fraction(2)]))], [
                ui.stack([.Background(.Rgb(200, 40, 40)), .Height(.Px(36))], []),
                ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(36))], []),
                ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(36))], []),
            ]),
        ),
    ])
}

/// A share is a share of what the fixed and the content-sized tracks left,
/// which is the whole of what makes a mixed grid different from three of a
/// kind.
export fn mixedTracks<C>(): Node<C> {
    page([
        band(
            "Fixed(.Px(120)), Auto, Fraction(1)",
            ui.stack([.Layout(.Grid([.Fixed(.Px(120)), .Auto, .Fraction(1)]))], [
                ui.stack(
                    [
                        .Background(.Rgb(200, 40, 40)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("fixed"))],
                ),
                ui.stack(
                    [
                        .Background(.Rgb(40, 150, 40)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("auto"))],
                ),
                ui.stack(
                    [
                        .Background(.Rgb(40, 40, 200)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("fraction"))],
                ),
            ]),
        ),
        band(
            "Auto, Fraction(1), Fixed(.Px(100))",
            ui.stack([.Layout(.Grid([.Auto, .Fraction(1), .Fixed(.Px(100))]))], [
                ui.stack(
                    [
                        .Background(.Rgb(200, 40, 40)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("fixed"))],
                ),
                ui.stack(
                    [
                        .Background(.Rgb(40, 150, 40)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("auto"))],
                ),
                ui.stack(
                    [
                        .Background(.Rgb(40, 40, 200)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("fraction"))],
                ),
            ]),
        ),
        band(
            "Fraction(1), Fixed(.Px(200)), Fraction(3)",
            ui.stack([.Layout(.Grid([.Fraction(1), .Fixed(.Px(200)), .Fraction(3)]))], [
                ui.stack(
                    [
                        .Background(.Rgb(200, 40, 40)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("fixed"))],
                ),
                ui.stack(
                    [
                        .Background(.Rgb(40, 150, 40)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("auto"))],
                ),
                ui.stack(
                    [
                        .Background(.Rgb(40, 40, 200)),
                        .Foreground(.Rgb(255, 255, 255)),
                        .Padding(.Px(4)),
                    ],
                    [ui.text(.Const("fraction"))],
                ),
            ]),
        ),
    ])
}

/// `Span` over four even tracks of two hundred. The first block covers as
/// many as it asks for and the three behind it shuffle along.
export fn span<C>(): Node<C> {
    page([
        band(
            "Span(1) on the first cell",
            ui.stack(
                [
                    .Layout(
                        .Grid([.Fraction(1), .Fraction(1), .Fraction(1), .Fraction(1)]),
                    ),
                ],
                [
                    ui.stack(
                        [.Background(.Rgb(200, 40, 40)), .Height(.Px(30)), .Span(1)],
                        [],
                    ),
                    ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(30))], []),
                    ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(30))], []),
                    ui.stack([.Background(.Rgb(230, 160, 20)), .Height(.Px(30))], []),
                ],
            ),
        ),
        band(
            "Span(2) on the first cell",
            ui.stack(
                [
                    .Layout(
                        .Grid([.Fraction(1), .Fraction(1), .Fraction(1), .Fraction(1)]),
                    ),
                ],
                [
                    ui.stack(
                        [.Background(.Rgb(200, 40, 40)), .Height(.Px(30)), .Span(2)],
                        [],
                    ),
                    ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(30))], []),
                    ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(30))], []),
                    ui.stack([.Background(.Rgb(230, 160, 20)), .Height(.Px(30))], []),
                ],
            ),
        ),
        band(
            "Span(3) on the first cell",
            ui.stack(
                [
                    .Layout(
                        .Grid([.Fraction(1), .Fraction(1), .Fraction(1), .Fraction(1)]),
                    ),
                ],
                [
                    ui.stack(
                        [.Background(.Rgb(200, 40, 40)), .Height(.Px(30)), .Span(3)],
                        [],
                    ),
                    ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(30))], []),
                    ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(30))], []),
                    ui.stack([.Background(.Rgb(230, 160, 20)), .Height(.Px(30))], []),
                ],
            ),
        ),
    ])
}

/// A gap in a grid comes out of the room the shares divide, so three even
/// tracks get narrower as the gap grows.
export fn gridGaps<C>(): Node<C> {
    page([
        band(
            "Fraction(1) three times, Gap(.Px(0))",
            ui.stack(
                [
                    .Layout(.Grid([.Fraction(1), .Fraction(1), .Fraction(1)])),
                    .Gap(.Px(0)),
                ],
                [
                    ui.stack([.Background(.Rgb(200, 40, 40)), .Height(.Px(36))], []),
                    ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(36))], []),
                    ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(36))], []),
                ],
            ),
        ),
        band(
            "Fraction(1) three times, Gap(.Px(12))",
            ui.stack(
                [
                    .Layout(.Grid([.Fraction(1), .Fraction(1), .Fraction(1)])),
                    .Gap(.Px(12)),
                ],
                [
                    ui.stack([.Background(.Rgb(200, 40, 40)), .Height(.Px(36))], []),
                    ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(36))], []),
                    ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(36))], []),
                ],
            ),
        ),
        band(
            "Fraction(1) three times, Gap(.Px(40))",
            ui.stack(
                [
                    .Layout(.Grid([.Fraction(1), .Fraction(1), .Fraction(1)])),
                    .Gap(.Px(40)),
                ],
                [
                    ui.stack([.Background(.Rgb(200, 40, 40)), .Height(.Px(36))], []),
                    ui.stack([.Background(.Rgb(40, 150, 40)), .Height(.Px(36))], []),
                    ui.stack([.Background(.Rgb(40, 40, 200)), .Height(.Px(36))], []),
                ],
            ),
        ),
    ])
}
