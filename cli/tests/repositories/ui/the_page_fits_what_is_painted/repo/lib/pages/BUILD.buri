# No `platforms` here, and that is the case: a snapshot is painted by the native
# runtime, so this suite has to be the linked binary rather than a JavaScript
# module.
library {
    sources: ["pages.buri"]

    test {
        sources: ["test/pages.buri"]
    }
}
