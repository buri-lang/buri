# One library and one test source: six pictures of a `link`, over two boxes of
# text, and a snapshot each.
library {
    sources: ["scenes.buri"]

    test {
        sources: ["test/pictures.buri"]
    }
}
