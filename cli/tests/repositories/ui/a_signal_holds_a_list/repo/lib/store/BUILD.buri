# No `platforms` here, and that is the case: the reactive graph is the native
# runtime's, so this suite has to be the linked binary rather than a JavaScript
# module.
library {
    sources: ["store.buri"]

    test {
        sources: ["test/store.buri"]
    }
}
