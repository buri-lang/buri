# One library and one test source: six pictures of a `field`, whose styles land
# on the input rather than on the label beside it, and a snapshot each.
library {
    sources: ["scenes.buri"]

    test {
        sources: ["test/pictures.buri"]
    }
}
