// Reaches its own library, which is exempt, and //lib/helper, which is not.

from "//lib/subject/lib.buri" import { triple };
from "//lib/helper/lib.buri" import { double };
from "core/testing/assert/lib.buri" import * as assert;

test "triple, checked against double" {
  assert.eq(triple(2), 6);
  assert.eq(double(3), 6);
}
