# Declares //lib/a and does not import it, which is what closes the cycle in
# the graph. The recorded goldens show the cycle reported from each end.
library {
    sources: ["b.buri"]
    dependencies: ["//lib/a"]
    visibility: ["//visibility:public"]
}
