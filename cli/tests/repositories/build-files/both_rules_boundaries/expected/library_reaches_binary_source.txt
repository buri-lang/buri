error: //tools/report/flags belongs to the binary in //tools/report [binary-source-import]
 --> tools/report/detail.buri:6:6
  |
6 | from "//tools/report/flags.buri" import { label };
  |      ^^^^^^^^^^^^^^^^^^^^^^ a library may not reach the binary beside it
  |
  = tools/report/detail.buri belongs to the library rule, and the binary depends on the library rather than the other way round
  = fix: move what you need into the library, or into a third one both can depend on
warning: label is imported but not used [unused-import]
 --> tools/report/detail.buri:6:38
  |
6 | from "//tools/report/flags.buri" import { label };
  |                                      ^^^^^
  |
  = Unused imports make the dependency graph messy and makes the codebase harder to maintain and clean up
  = fix: remove the import
