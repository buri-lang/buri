from "//lib/probe/lib.buri" import { probe };
from "//lib/probe/testing/lib.buri" import { sample };
from "core/testing/assert/lib.buri" import * as assert;

test "the probe is shallower than the sample" {
  assert.isTrue(probe() < sample());
}
