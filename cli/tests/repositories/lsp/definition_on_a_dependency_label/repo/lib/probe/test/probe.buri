from "core/testing/assert" import * as assert;
from "//lib/probe" import { probe };
from "//lib/probe/testing" import { sample };

test "the probe is shallower than the sample" {
    assert.isTrue(probe() < sample());
}
