// The surface of the schema: a `.proto` module is internal to the rule that
// declared it, so this is what lets anything else name what it holds.
from "//lib/wire/point.proto" export { Point, defaultPoint };
