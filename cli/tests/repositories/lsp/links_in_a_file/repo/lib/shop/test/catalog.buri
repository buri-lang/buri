from "core/testing/assert" import * as assert;
from "//lib/shop" import { shopfront };

test "the shopfront is where the doc comment says" {
    assert.equal(shopfront(), "https://example.org/shop");
}
