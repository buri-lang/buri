from "core/testing/assert/lib.buri" import * as assert;
from "//lib/shop/lib.buri" import { shopfront };

test "the shopfront is where the doc comment says" {
    assert.eq(shopfront(), "https://example.org/shop");
}
