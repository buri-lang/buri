from "//lib/counter/lib.buri" import { Tally, counter };
from "core/testing/assert/lib.buri" import * as assert;

test "counter increments" {
  let tally = Tally { total: 1 };
  assert.eq(counter(tally), 1);
}
