# Schema: the repository-case manifest, cli/tests/harness/case.rs. Textproto,
# but not a Buri file: only repo/ below is a Buri repository.

doc: "the requests whose complete answer for Buri is an empty one — a linked edit the language has no construct for, inline values with no debugger to stop, and the two resolve requests there is nothing left to resolve for"

run {
  args: ["lsp"]
  exit: 0
  stdin: "session.jsonl"
  stream: OUT
  golden: "session.txt"
}
