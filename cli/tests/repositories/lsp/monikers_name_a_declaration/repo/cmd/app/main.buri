from "core/effect" import { Alloc, Stdout };
from "core/host" import * as host;
from "core/io" import * as io;
from "//lib/shop" import { currency, listing, worth };

export fn main(): Result<(), Str> {
    let ctx = context {
        Alloc: host.alloc,
        Stdout: host.stdout,
    };
    let shelf = [listing("plain", 100)];
    io.println(ctx, "${worth(shelf).show(ctx)} ${currency}").mapErr(fn(_e) => "could not write to standard output")
}
