from "//lib/shop/testing/lib.buri" import { sampleItem };
from "//lib/shop/lib.buri" import { subtotal };
from "core/testing/assert/lib.buri" import * as assert;

test "one sample item is worth nothing until it is stocked" {
  assert.eq(subtotal([sampleItem()]), 0);
}
