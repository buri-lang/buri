// The library. Nothing here is a finding: what `--fix` rewrites in this case
// is the suite and the fakes beside it.

derive Equal, Show for Cents;
export struct Cents(export I64);

export fn fromCents(c: I64): Cents {
    Cents(c)
}
