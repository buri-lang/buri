// A fake with an import nothing uses. `--fix` rewrites this file the same way
// it rewrites a library source.

from "core/str" import * as str;
from "//lib/money" import { Cents, fromCents };

export fn fakeCents(): Cents {
    fromCents(150)
}
