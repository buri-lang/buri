// The library under test. Nothing here is a finding: the `testing/` half
// beside it is what this case is about.

derive Eq, Show for Cents;
export struct Cents(export I64);

export fn fromCents(c: I64): Cents {
    Cents(c)
}
