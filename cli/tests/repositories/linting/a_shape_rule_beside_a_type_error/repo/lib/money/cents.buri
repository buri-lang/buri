// Six parameters and a body that returns the wrong type. The parameter count
// is written in the declaration, so the rule that counts them needs nothing
// from the checker and answers the same whether the body checks or not.

export fn describe(a: I64, b: I64, c: I64, d: I64, e: I64, f: I64): Str {
    a
}
