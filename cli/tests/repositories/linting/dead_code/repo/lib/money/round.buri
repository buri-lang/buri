// Imports `Cents` from a sibling, which is what keeps `Cents`'s own export
// reachable even though `lib.buri` also re-exports it. Nothing here names
// `toDollars`.

from "//lib/money/cents.buri" import { Cents };

export fn toNearestDollar(c: Cents): Cents {
    Cents(c.0 - c.0 % 100)
}
