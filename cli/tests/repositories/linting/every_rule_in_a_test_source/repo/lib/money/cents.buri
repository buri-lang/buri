// The library under test. Nothing here is a finding: the suite beside it is
// what this case is about, and a library with a finding of its own would make
// the report ambiguous about which half was being read.

derive Eq, Show for Cents;
export struct Cents(export I64);

export fn fromCents(c: I64): Cents {
    Cents(c)
}
