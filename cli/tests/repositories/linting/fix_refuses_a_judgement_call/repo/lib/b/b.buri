// Genuinely imports //lib/a, so the dependency is real and `unused-dep` does
// not fire. What is left is a cycle, and a cycle has no mechanical answer:
// which of the two edges to cut is a design decision.

from "//lib/a" import { callB };

export fn fromB(): Str { "b" }

export fn viaA(): Str { callB() }
