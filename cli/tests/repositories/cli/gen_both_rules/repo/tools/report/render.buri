from "core/effect/lib.buri" import { Alloc };
from "core/str/lib.buri" import * as str;

from "//tools/report/shared.buri" import { heading };

export fn render<C: Alloc>(ctx: C, total: Int): Str {
  str.format(ctx, "${heading()} ${total}")
}
