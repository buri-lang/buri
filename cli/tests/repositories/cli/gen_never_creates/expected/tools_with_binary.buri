# An empty rule is enough to start. It is also the whole of what this package
# declares: there is a `main.buri` in this directory and no `binary` rule, and
# `buri gen` must not decide that is an oversight.
library {
    sources: ["helper.buri"]
}

binary {
    outputs: [
        { platform: JS },
    ]
}
