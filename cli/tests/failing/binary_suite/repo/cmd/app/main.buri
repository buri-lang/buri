from "core/cap" import { Alloc, Stdout };
from "core/host" import * as host;

export fn greeting(): Str {
    "hello"
}

export fn main(): Result<(), Str> {
    let ctx = context {
        Alloc: host.alloc,
        Stdout: host.stdout,
    };
    let _ = ctx.println(greeting());
    .Ok(())
}
