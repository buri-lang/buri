from "core/effect" import { Allocator, Stdout };
from "core/io" import * as io;

/// Writes, and answers something a test can be wrong about.
export fn announce<C: Allocator + Stdout>(ctx: C, name: Str): Int {
    let _ = io.println(ctx, "announcing ${name}").ignore();
    name.len()
}
