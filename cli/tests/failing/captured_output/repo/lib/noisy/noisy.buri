from "core/effect" import { Alloc, Stdout };

/// Writes, and answers something a test can be wrong about.
export fn announce<C: Alloc + Stdout>(ctx: C, name: Str): Int {
    let _ = ctx.println("announcing ${name}");
    name.len()
}
