// EXPECT: this `match` does not cover `.Circle`
// ISSUE: recorded as it reads today; filed as https://github.com/buri-lang/buri/issues/110
export enum Shape {
  Circle,
  Square,
}

export fn sides(shape: Shape): Int {
  match (shape) {
    Circle. => 1,
    .Square => 4,
  }
}
