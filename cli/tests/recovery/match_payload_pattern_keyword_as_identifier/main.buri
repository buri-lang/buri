// EXPECT: expected an identifier, found `match`
export enum Shape {
  match(Int, Int),
  Empty,
}

export fn size(shape: Shape): Int {
  match (shape) {
    .match(x, y) => x + y,
    .Empty => 0,
  }
}
