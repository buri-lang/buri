// EXPECT: `Str` has no variant `strformat`
from "core/effect" import { Allocator };
from "core/str" import * as str;

export fn line<C: Allocator>(ctx: C, name: Str, place: Str): Str {
  .strformat(ctx, "${name} of ${place}")
}
