// EXPECT: a variant-pattern field ends with `,`
export enum Shape {
  Point(Int, Int),
  Empty,
}

export fn size(shape: Shape): Int {
  match (shape) {
    .Point(x y) => x + y,
    .Empty => 0,
  }
}
