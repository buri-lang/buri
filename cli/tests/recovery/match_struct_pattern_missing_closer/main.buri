// EXPECT: this variant field pattern is missing its closing `}`
export struct Point {
  export x: Int,
  export y: Int,
}

export fn sum(point: Point): Int {
  match (point) {
    Point { x, y  => x + y,
  }
}
