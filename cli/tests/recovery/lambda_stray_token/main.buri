// EXPECT: expected a pattern, found `@`
// ISSUE: recorded as it reads today; filed as https://github.com/buri-lang/buri/issues/114
export fn twice(n: Int): Int {
  let @ double = fn(n: Int): Int => n * 2;
  double(n)
}
