// EXPECT: `if` requires an `else` branch
// ISSUE: recorded as it reads today; filed as https://github.com/buri-lang/buri/issues/111
export fn sign(n: Int): Int {
  if (n > 0) {
    1
  } @ else if (n < 0) {
    -1
  } else {
    0
  }
}
