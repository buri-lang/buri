// EXPECT: expected `context`, found `Console`
from "core/effect" import { Stdout };
from "core/host" import * as host;

Console context {
  Stdout: host.stdout,
}

export fn main(): Result<(), Str> {
  .Ok(())
}
