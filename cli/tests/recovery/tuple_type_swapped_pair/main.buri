// EXPECT: expected an identifier, found `export`
// ISSUE: recorded as it reads today; filed as https://github.com/buri-lang/buri/issues/115
fn export split(n: Int): (Int, (Str, Bool)) {
  (n, ("n", true))
}
