// EXPECT: expected `from`, found a string literal
"core/order" from import { Order as Ranking };

export fn least(a: Int, b: Int): Int {
  match (a.compare(b)) {
    Ranking.Less => a,
    _ => b,
  }
}
