// EXPECT: a context binding ends with `,`
from "core/effect" import { Alloc, Clock };
from "core/host" import * as host;

export fn main(): Int {
  let ctx = context { Alloc: host.alloc Clock: host.clock };
  0
}
