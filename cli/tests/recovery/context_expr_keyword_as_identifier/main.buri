// EXPECT: expected an identifier, found `match`
from "core/effect" import { Allocator, Clock };
from "core/host" import * as host;

export fn main(): Result<(), Str> {
  let ctx = context { Allocator: host.match, Clock: host.clock };
  let _ = ctx;
  .Ok(())
}
