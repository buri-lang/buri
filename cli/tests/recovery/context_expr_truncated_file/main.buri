// EXPECT: expected an identifier, found end of file
from "core/effect" import { Allocator, Clock };
from "core/host" import * as host;

export fn main(): Result<(), Str> {
  let ctx = context { Allocator: host.
