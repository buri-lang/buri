// EXPECT: this variant field pattern is missing its closing `}`
export enum Shape {
  Rect { width: Int, height: Int },
  Empty,
}

export fn area(shape: Shape): Int {
  match (shape) {
    .Rect { width, height  => width * height,
    .Empty => 0,
  }
}
