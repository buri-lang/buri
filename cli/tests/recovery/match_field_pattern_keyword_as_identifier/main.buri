// EXPECT: expected an identifier, found `match`
export enum Shape {
  match { width: Int, height: Int },
  Empty,
}

export fn area(shape: Shape): Int {
  match (shape) {
    .match { width, height } => width * height,
    .Empty => 0,
  }
}
