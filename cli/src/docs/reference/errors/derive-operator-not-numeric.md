---
title: A derived arithmetic operator is the operation on the wrapped value
message: '`{type}` derives `{operator}` only for a struct wrapping a number, not `{wrapped}`'
note: a derived arithmetic operator is the operation on the wrapped value
fix: write the `impl` by hand, or wrap a numeric type
reproduction: none
---

