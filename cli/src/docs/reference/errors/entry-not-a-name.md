---
title: An output's `entry` names a function
message: '`{entry}` is not a function name'
note: an `entry` names an exported function of the binary's `main.buri`, so it is a Buri identifier — letters, digits and `_`, not starting with a digit
fix: write the function's name, or drop `entry` to enter through `main`
reproduction: none
---
