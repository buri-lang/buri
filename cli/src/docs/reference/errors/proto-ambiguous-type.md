---
title: A type name says which schema it means
message: '`{name}` is ambiguous'
note: it could be {candidates}
fix: write the fully-qualified name, package and all, so the field says which one it means
reproduction: none
---
