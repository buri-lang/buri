---
title: A test title is one line
severity: warning
message: test {quoted_title} has a newline in its title
note: a failure report is one line per test, so the runner prints the title escaped — the break shows up as `\n` rather than as a line break
fix: write the title on one line; a sentence is enough, and the body says the rest
---
