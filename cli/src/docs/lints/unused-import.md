---
title: Every import is used
message: "`{name}` is imported and never used"
note: an import that names something the module does not use makes the dependency graph describe something other than the code
fix: remove `{name}` from the import
---
