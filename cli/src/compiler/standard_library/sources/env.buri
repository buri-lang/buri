//! core/env — environment variables and process arguments.

from "core/effect" import { Env };

export fn get<C: Env>(ctx: C, name: Str): Option<Str> {
  ctx.variable(name)
}

export fn args<C: Env>(ctx: C): [Str] {
  ctx.args()
}
