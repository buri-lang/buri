// The surface of //tools/report. The binary next door reaches the library
// through this file too — a library boundary is a property of the library, not
// of the directory, so it holds even for a file sitting beside it.

from "./render" export { render };
