# The decision index

Everything in `design/native/` is decided. This file is the index: one row per
decision that was close enough to be worth arguing, the document where it is
argued in full, and the alternative that lost.

It exists because the arguments are spread across six documents and a reader
who wants to know *whether* something was considered should not have to read all
seven to find out. Where a row and its document disagree, the document is right —
this is a table of contents, not a second source.

Nothing here is open. Work that is not done is in
[`design/TODO.md`](../TODO.md), under "The native backend"; a decision that is
reversed is reversed in the document that made it, with the reversal recorded
there rather than deleted.

| Decision | Where | The alternative that lost |
|---|---|---|
| Reference counting, not a GC | MEMORY.md §3 | A tracing GC needs statepoints or conservative scanning; the first contradicts CODEGEN-LLVM.md §0's second instruction, the second makes the heap data-dependent and irreproducible. |
| `Checked` is bounded by the numbers the *backend* has | VALUE-MODEL.md §11, SPEC §6.2.2 | A native backend that also stopped at 2^53 shipped for a wave and was reversed: it makes `Checked` useless on `I64` natively, and buys portability of a result nobody should branch on. |
| `Str` slicing keeps its parent buffer alive, and that is not a language question | MEMORY.md §5.3, §6 | Copying above a ratio makes `slice` name `Alloc` and retracts `core/str`'s purity claim; copying on proven retention is a real analysis for a case that is hard to detect, and stays available precisely because it is not language-visible. |
| Exactly one supported LLVM at a time, tracking the latest both inkwell and the flake carry | CODEGEN-LLVM.md §8.1–8.3 | "The flake leads" forbids a bump the backend needs; "the backend leads" prices a nixpkgs bump — which moves `cargo`, `bun`, `elan` and therefore `buri`'s own hash — into a routine chore. Multi-version support is refused permanently: two LLVMs means one attribute encoded two ways. |
| No arena per `Alloc` scope | MEMORY.md §4 | `Alloc` is a propagating bound and `Region` is a value; there is no scope to hang an arena on. |
| The middle end owns tail-call elimination; no backend uses its own | CODEGEN-LLVM.md §5 | `musttail`/`tailcc` and `return_call` are both viral, both incompatible with a C-ABI runtime, and both unnecessary once the tail-call graph is a DAG. The `return_call` half was argued in CODEGEN-CRANELIFT.md §3.3 and went with that document on 2026-08-29 (CODEGEN-STENCIL.md §13); the row is unchanged, because the reason never rested on it. |
| Neither linker links incrementally; recompile granularity is the codegen unit | CODEGEN-STENCIL.md §12 | mold rejected incremental linking partly *because it is not reproducible*, which this toolchain cannot accept. |
| A `stat` sweep over the declared source set, not `notify` | BUILD-AND-WATCH.md §1.2 | A general watcher is hard (Zed abandoned its own; Zig abandoned kqueue) and unnecessary when the input set is enumerated by the cache keys already. |
| One codegen unit per source module, in both profiles; no LTO | ARCHITECTURE.md §5 | Per-function objects lose colocated direct calls; a merged fixed count loses reuse; LTO re-derives an inlining decision the middle end took with an exact call graph. |
| `Str` is 24 bytes with a `base` pointer; `[T]` is 16 | VALUE-MODEL.md §3-4 | A uniform 16-byte shape would make `slice` allocate, contradicting `core/str`'s own contract. |
| Descriptors are folded at compile time and never reach a native artifact | VALUE-MODEL.md §9 | A runtime walker is an interpreter, defeats every memory attribute, and defeats DCE. |
| A context of zero-sized implementations costs nothing | VALUE-MODEL.md §8 | Nothing lost; `ctx_layouts` already records the layout statically. |
| `nsw`/`nuw` are never emitted | CODEGEN-LLVM.md §3.4 | Correct per SPEC 6.2, and it would replace a debuggable wrap with whatever the optimizer inferred. |
| `willreturn` only where the backend proved there is no cycle | CODEGEN-LLVM.md §3.6 | Reading the design's *"a function `middle` proved terminates"* as `Purity::Pure` with no abort — which miscompiled every pure non-tail recursion at `default<O2>`: `memory(none)` + `willreturn` is `speculatable`, and LLVM hoisted the recursive call above its own base case. Also rejected: keeping the attribute for a loop, which is `mustprogress` by another name and licenses deleting a divergence SPEC 10.4 calls observable. The row a decreasing-measure analysis in `middle` would take back is named in §3.6 rather than claimed. |
| Cranelift pinned to the wasmtime 36 LTS — **reversed 2026-08-29**, with its subject | CODEGEN-STENCIL.md §13 | Tracking latest meant a monthly `identity()` change, which invalidated every cached object in every repository. The pin was answering a question the removal deleted: there is no version left to bump. |
| `backend-llvm` off by default; `backend-stencil` on | BUILD-AND-WATCH.md §2 | `cargo install buri` must not need a system LLVM. `backend-cranelift` was on by default beside `backend-stencil` until **2026-08-29** (CODEGEN-STENCIL.md §13); what is left needs nothing a machine able to link a native artifact does not already have, and adds nothing to the lockfile. |
| The stencil backend is the debug backend — **reversed 2026-08-29** | ARCHITECTURE.md §4, CODEGEN-STENCIL.md §13 | It was compiled in and never selected for as long as the seat was a decision about parity rather than about plumbing. Parity was met — 997 of 997, the same six refusals, the same blocks live at exit — so the decision was taken and Cranelift was removed: 38 transitive crates to 0, a clean release build in half the time, and x86-64 covered by writing the emitter rather than by keeping a second code generator. What lost, and is written down rather than repaired: a 1.38× debug runtime, and a `buri` 5.06 MB *larger* rather than smaller — the machine code halved but the stencil libraries the old binary dead-stripped are the code generator now (PERFORMANCE.md §5). The one behavioural difference the flip would have carried — `str.concat` allocating unconditionally — was closed before the flip rather than accepted with it (CODEGEN-STENCIL.md §5.0.1). |
| Cross-*linking* refused, explicitly; cross codegen supported | ARCHITECTURE.md §9 | The blocker is the host-only runtime archive rather than any backend, and naming that makes the fix a packaging problem rather than a compiler one. |
| `Arena` is accounting-only in v1 | MEMORY.md §7.2 | A real region-freeing arena needs a scoped-context construct the language does not have. |
| One symbol prefix, `buri_rt_`, for every runtime entry including the host ones | VALUE-MODEL.md §10 | A split `buri_rt_` / `buri_host_` makes "is this symbol the runtime's" a table lookup in the one place a compiler asks it per call site. |
| The v1 allocator is `malloc`-backed with no size classes | MEMORY.md §5.4 | Nothing observable differs — the header, the capacity, the reuse test and the *defined* `Alloc` cost model are identical — so the free lists can land later under a green suite instead of being co-developed with two backends. |
| The runtime archive is precompiled by `cli/build.rs`, not compiled on demand | BUILD-AND-WATCH.md §2.2 | On demand puts a `rustc` in the build loop and makes the archive hash depend on whatever `rustc` was on `PATH` at *use* time. |
| `Net::fetch` is cleartext `http://` only, and says so — **reversed 2026-08-30**, with its subject | `cli/runtime/http.rs`, `cli/runtime/tls.rs`, BUILD-AND-WATCH.md §1.1.2 | `https://` works. It goes through `rustls` over the `ring` provider, the server's certificate is checked against the host's trust anchors, and a certificate that does not check out is a `NetError::Transport` naming what was wrong and which trust set it was checked against. The half of the original row that never depended on the refusal still stands and is now structural: a client that silently downgraded to cleartext would be worse than one that refuses, and `http.rs` cannot downgrade, because the scheme decides one thing — whether the socket is wrapped — and nothing else. What the reversal costs is 1.72 MiB in every binary this compiler produces (§1.1.2), which is over the size budget CI carried and is re-stated there rather than absorbed. A `net`-off toolchain keeps the refusal, and its message names the feature. |
| Every step of a native `Net.fetch` is bounded, the **name lookup** included, and the lookup's bound is a thread the caller stops waiting on | `cli/runtime/http.rs`'s `DEADLINE`, `within` and `resolve`; `cli/runtime/tls.rs`'s `bound` | `connect_timeout` and the socket's own read and write deadlines cover four of the five steps, and the fifth cannot be cancelled at all: `getaddrinfo` is in the kernel and comes back when the resolver lets it. So a host whose DNS is answered by nothing had a `Net.fetch` that never returned, on a thread that is a carrier and not the caller's to lose. The alternative was to leave the lookup unbounded and let callers bound it — which is what a runtime unit test was doing when it hung a CI job for an hour, and what every future caller would have to remember. Waiting on a thread costs one thread per lookup that has a name in it (an address literal skips the arrangement entirely) and buys a client that returns on every network, including one that drops the packets. What it does not buy is cancellation: the abandoned lookup finishes into a channel nobody is holding. |
| One bar, **two** admitted sets: the toolchain's and the runtime's — **amended 2026-08-30** | root `Cargo.toml`, BUILD-AND-WATCH.md §1.1, §1.1.1 | One list would have made "tokio is in the compiler" and "tokio is in every program the compiler produces" the same sentence, and they are not the same decision: `libburi_rt.a` is linked into every native binary, so the runtime's set is a set strangers ship. The amendment is a split, not a weakening — the same three clauses, plus a fourth on the runtime's side that the toolchain's does not carry: the set is closed by an **exact list**, asserted as an equality, so a fifth crate and a removal both fail a test. The alternative that lost was one bar and one list with the runtime's entries marked, which loses the fourth clause and the asymmetry that motivates it. |
| The runtime's four networking crates land **referenced by nothing** — **amended 2026-08-30** three times; there are six crates now and three of them are reached | `cli/runtime/net.rs`, `manifest.toml`, `rt.rs`, `tls.rs` | Landing them with their first caller would have made the archive's growth and the code that needed it one commit, and the growth is the part that is irreversible: the archive is `include_bytes!`d into every `buri` binary. Measured first instead — 5 987 472 → 5 987 496 bytes, twenty-four, because fat LTO drops what nothing reaches — and held there by a CI symbol-table check that each later slice had to move deliberately. Both did. B6 linked the reactor: `rt.rs`, 6 035 480 → 6 220 904 bytes, +185 424 on aarch64-apple-darwin. C7 linked the TLS client, and added `ring` as the fifth crate already reached: `rustls` and `ring` left the "must be absent" list and joined a "must be present" one in the commit that reached them, +1 804 592. `hyper` and `tungstenite` are still referenced by nothing and the check still says so. The convention worked exactly as designed both times — each figure was a number in a report before it was a number in a binary, and the 1.72 MiB is what moved the size budget rather than what was discovered under it. C4 then added `quinn` the same way and the convention paid again in the other direction: an archive built with `net-h3` is 8 198 992 bytes against 8 199 032 without it — **forty bytes smaller**, the difference being a refusal string an h3 build has no use for — so HTTP/3's real price is a number F2 will produce rather than one this slice had to guess, and `quinn` joins the "must be absent" list on every leg instead of moving a budget. |
| The archive's feature list is a **file beside the archive**, and the refusal for a missing one is a **diagnostic before codegen** | `cli/build.rs`, `backend/runtime_native.rs`, `backend/mod.rs`, `docs/errors/networking-not-available.md` | Three ways the toolchain could learn whether its runtime has `net` were open: a `cargo:rustc-env`, a byte written beside the archive, or reading `buri_rt_net_capabilities` back out of the archive's symbol table. The file wins for the reason the baked digest is a file — one `OUT_DIR` written by one run of the build script, so a stale directory cannot pair one build's bytes with another's answer — and it generalises to `net-h3` as a second line rather than a second mechanism. Reading the symbol table would make the toolchain parse `ar` and Mach-O/ELF to learn something the script already knew. What it is *for* is the second half: `Backend::missing_intrinsics` reports the `host.HostListen.*`/`HostSockets.*`/`HostTasks.*` family as missing when the archive has no `net`, so the failure is a sentence naming the operations and the feature, before code generation, rather than an unresolved `buri_rt_*` symbol from `cc`. The refusal landed **before the keys exist**, on purpose: the alternative is that the first toolchain built without networking meets them as a link error. |
| `net-h3` is **off by default**, and a toolchain without it **returns `.Err(Unsupported)`** rather than refusing the program | `cli/runtime/manifest.toml`, `cli/runtime/net.rs`'s `serves`, `cli/build.rs`, BUILD-AND-WATCH.md §1.1.3 | Two decisions, and they are the same one twice. The default is opposite `net`'s because the payer is different: `net`'s five crates are what any program that speaks the network needs, while `quinn` is what a program that **asked for HTTP/3** needs, and the note's gate was "behind configuration until the crate is trusted" — so a user who never writes `.Http3` neither resolves nor ships a QUIC stack. The refusal is a **value** for the reason `host.HostNet.fetch` was kept out of `net_intrinsic` when `https://` landed: the keys an HTTP/3 server is reached through are `HostListen`'s, the protocol is a *field* rather than an operation, and a compile-time refusal would therefore have to refuse every program that mentions `serve` — including every server that was only ever going to speak HTTP/1.1. The two alternatives both lost on that: a `missing_intrinsics` entry is over-broad, and an abort turns a build-flag question into a dead process, which `lib.rs` §5 reserves for a broken invariant. `Server` and `serve` do not exist yet (F2), so what landed is the machinery under them — the feature, the `net-h3` line in `libburi_rt.a.features`, `runtime_native::h3()`, the `buri_rt_net_h3_available` door and `net::serves` — and F2 spends it in one line, `net::serves(protocol)?`. |
| `quinn`'s crypto provider is pinned to **`ring`**, and CI greps for the other one | `cli/runtime/manifest.toml`, `.github/scripts/assert-runtime-archive.sh` | `quinn`'s own default features are `rustls-aws-lc-rs` and `platform-verifier`; taking them would have put a **second** cryptography implementation into every h3 binary beside the one `rustls` already uses, and `aws-lc-rs` additionally wants `cmake` at build time, which is the tool C7 rejected it over. Both defaults are turned off and `rustls-ring` is turned on, so the closed set grows by one crate rather than by a provider and its build tooling. The `aws_lc` tripwire C7 added to the archive script — a name that was never a dependency — is what checks it, and `quinn` is exactly the crate it was watching for; the h3 leg is the first one where that grep can fail. |
| `net-h3` is built on **one CI job**, and its archive is held to the **same size budget** | `.github/workflows/ci.yml`, `.github/scripts/assert-runtime-archive.sh` | Nothing about the question is per-platform: `quinn` is portable Rust reaching the archive through the same nested `cargo` the other five do, and what is asserted is a symbol table and a byte count rather than a syscall. Four jobs would buy the same answer four times. It is the `x86_64` `test` leg's **last** step because it rebuilds the archive and relinks `buri`, so anything after it would measure a toolchain no other job has. The budget is shared rather than widened for the same reason the row above gives — the h3 archive is smaller — and a separate, larger h3 number would have been a number with nothing behind it that hid the growth F2 will cause. |
| Native suspension is a **carrier thread plus a run baton**, not a CPS transform | `cli/runtime/rt.rs`, design/native track B §0.1 | Buri machine code stays ordinary frame-threaded synchronous code and a suspending host call is one `park_on`. The baton — exactly one carrier in Buri code at a time — is what lets *concurrency* land while *parallelism* does not, so non-atomic refcounts, the single-threaded allocator and every `rc == 1` in-place update stay correct untouched and atomic RC lands as its own priced slice (track G) rather than as everyone's prerequisite. The alternative, a native CPS/state-machine transform, is larger than the two shapes `middle/mod.rs` and CODEGEN-LLVM.md have each already rejected, put together. Phase 2 (B9) replaces the thread with a stack switch behind the same `park_on`. **Amended 2026-08-30 (G3):** the carrier thread stays and **the baton is gone**. It was a staging device and it worked as one — it bought concurrency while parallelism waited, and it let atomic reference counting land as its own priced slice rather than as everyone's prerequisite. What replaces it is not another lock: the values two carriers can both reach are marked, a marked block is counted atomically and is never eligible for an in-place write, and `fan_out` is gated on the same mark, so the exclusion moved out of a lock held across a whole call and into the header word of the blocks that call touches — which is the only place it can be while two steps genuinely compute at once. `Baton`, `Ticket`, `Held` and `as_carrier` are deleted; `park_on` is `handle().block_on(future)`; `Tasks.parallel` on `--release` is parallel rather than concurrent. Phase 2 (B9) still replaces the thread with a stack switch behind the same `park_on`. **Amended 2026-08-30 (B9):** the baton went in G3 and **the carrier thread has now gone too**. A task is a machine stack, a Buri data-stack list and one saved stack pointer; `park_on` polls, and on `Pending` saves the stack and switches to the carrier, which goes back for other work. Nothing above `cli/runtime/rt.rs` changed — `park_on`, `on_carrier`, `Handoff`, `task_start`/`join`/`is_live` and `buri_rt_host_tasks_parallel` all keep their shapes, and `host.rs` was not edited — which is what "behind the same ABI" was supposed to mean and is why the benchmark's two columns are two runs of one source. A *carrier* is still a thread and there are still up to `MAX_CARRIERS` of them, because a task may block rather than park and one that does holds its carrier; what a task no longer costs is a thread while it **waits**. |
| The step ABI carries an **index word on every step**, not only on the keys that read one | `cli/runtime/list.rs`'s `StepEntry`, `backend/intrinsic_keys.rs` | `effect Tasks` promises the step its item's own index, and only the side driving the walk has one: deriving it inside the entry thunk means dividing `arg - base` by the stride, which needs a base the state record does not carry and which stops being true the moment a scheduler hands elements out of order — which is what D4 makes `parallel` do. Putting the word on `Tasks.parallel` alone was the alternative, and it costs *two* C signatures at a boundary whose whole value is that there is one: the runtime would have to know which shape a key is before it could call the thunk. `list.mapCtxStep`'s closure has no index parameter, so its thunk leaves the register unread — one register against a second ABI. |
| `Tasks` is granted on `LINUX`, `MACOS` and `JS`, and **withheld from `WEB`** | `standard_library/mod.rs`, `cli/tests/reject/tasks_not_granted_on_web` | Not a limitation of the backend: `WEB` and `JS` share a code generator, so `Promise.all` would run there. It is the judgement `Net`'s row made until `fetch` stopped waiting — `parallel` returns only when its last task has finished, and a page has an interface that a wait is visible in. Every effect a page's host does grant either answers instantly (`Ui`, `Watch`) or suspends without holding the event loop, which is what let `Net` onto `WEB`; granting a *waiting* effect there puts back the one shape that host is built to avoid, in the one place a program cannot leave. The callback-shaped task surface a page would want is the servers' slice, not this one. |
| `Tasks.parallel`'s body is in **`rt.rs`, behind `net`**, rather than beside the other list walks | `cli/runtime/rt.rs`, `backend/runtime_native.rs` | The body needs no reactor today, so a featureless home would have compiled. It would also have made `net_intrinsic`'s refusal — which names the `host.HostTasks.*` family and was written a wave before any of those keys existed — a lie for exactly as long as it took D4 to make it true again. Putting it here means the refusal was *exercised* by the first key that reached it (a `BURI_RUNTIME_NET=0` toolchain refuses a `core/tasks` program by name, before code generation) rather than re-argued later. **Amended 2026-08-30**: the body is a fan-out onto the carrier pool rather than a loop, which is what D4 made true; the placement argument is unchanged and is now load-bearing rather than anticipatory — the scheduler needs the reactor the feature admits. |
| Whether a fan-out is legal is the **artifact's statement about itself**, made once at startup, not a word on the step boundary | `cli/runtime/lib.rs` §6, `backend/llvm/runtime.rs`'s `FRAMES_PER_CARRIER` | `Tasks.parallel` may only run two steps at once where two steps may have live frames at once, and that is a fact about where a Buri frame lives. Under LLVM a frame is a machine frame, so a carrier's own thread stack is its own; under the frame-threaded backend a program has one `buri$stencil$stack` and an entry thunk works in a frame the *call site* set aside, so two steps of one `parallel` would share it and a step that suspends would still be holding it. `buri_rt_frames_are_per_carrier()` is therefore one optional call in the emitted `main`: silence is the safe answer and the old behaviour — steps in index order on the calling carrier — so an entry point that forgets it is slow and never wrong. The alternative was a **lane count on `Extra::Step`**: a fifth word saying how many steps this call site can hold frames for. It is the wrong altitude — the answer is the same at every call site in an artifact — and it widens a boundary two backends and two runtime entries have to agree on, `list.mapCtxStep` included, to carry a constant. B7 is what deletes this row rather than changes it: once each carrier acquires a Buri stack, both backends make the statement and the branch has one arm. |
| The fan-out is a **sliding window of 64 carriers**, not one carrier per item | `cli/runtime/rt.rs`'s `IN_FLIGHT` | A carrier is an OS thread with a 512 KiB stack, so the width is address space: 64 is 32 MiB per level of nesting. Unbounded is the shape that matches `Promise.all` exactly and is the wrong shape while a task costs a thread — a list of ten thousand items asks the kernel for ten thousand threads, which macOS refuses long before memory runs out, and `spawn_carrier` turns that refusal into an abort on a program JavaScript runs. The window costs the (n+1)-th task the wait for the first, and costs the answer nothing: the `[B]` is filled by index either way, which is the whole worth of the order promise. B9 removes the bound rather than raising it — a parked task that costs a stack switch and no thread has no reason to be counted. |
| `HostGrant::elsewhere_clause`'s **empty case is kept with no row using it** | `standard_library/mod.rs` | `Tasks` was the one ungrantable effect and is now granted, so nothing in the table exercises the branch, and the `!platforms.is_empty()` assertion D1 removed stays removed; the test builds a row of its own instead. Deleting the branch because its first user graduated would mean rediscovering "build this for a platform that grants it:" with nothing after the colon on the day the next effect lands ahead of its runtime — `Listen` and `Sockets` are both coming that way. Six lines and one test, against a diagnostic that reads like a compiler bug. |
| The carrier pool and the task table get **no `buri_rt_task_*` C ABI yet** — **amended 2026-08-30**, the file has its first export | `cli/runtime/rt.rs` §2 | An exported symbol is a contract both backends emit calls into. Writing one before a backend has a call to emit is a guess about a signature, and a wrong guess is a miscompile that shows up as a wrong answer at run time (`lib.rs` §1's whole argument). The table was Rust-visible in B6 and gains its first exported entry in track D, where the caller exists. D3 is that slice, and the convention held: `buri_rt_host_tasks_parallel` landed with both runtime-table rows and a caller in the same commit, and its C signature is what those rows describe rather than a prediction of one. The archive grew by the 544 bytes it costs, 6 220 904 → 6 221 448 on aarch64-apple-darwin. The *pool and the table* still have no C ABI: nothing yet starts a task from Buri code, and `task_start`/`task_join` stay Rust-visible until a backend has a call to emit for them too. |
| `rustls` is admitted with **no crypto provider** — **reversed 2026-08-30**; the provider is `ring` | `cli/runtime/manifest.toml`, `cli/build.rs`, BUILD-AND-WATCH.md §1.1.2 | Deferred until the bytes were bought rather than spent, which is now. `ring` over `aws-lc-rs` because `aws-lc-rs` wants `cmake` at build time and `ring` wants a C compiler the toolchain already requires for the stencil library and for every link; over the pure-Rust providers because `rustls-rustcrypto` says of itself that it is unaudited and not for production, and this archive is in every binary a stranger ships. The "turns degrades-rather-than-breaks into breaks" objection was real and is answered rather than accepted: `cli/build.rs` **probes** for `cc` before the build and falls back to `--no-default-features` with a `cargo:warning`, so a host without one gets a working toolchain, working `http://`, and an `https://` that refuses at run time naming the feature. A probe rather than a retry-on-failure, because retrying a failed compile with the feature off would also hide a bug in the runtime's own TLS code as a silently net-less toolchain. |
| Trust anchors come from the **host's PEM bundle**, with `SSL_CERT_FILE` overriding — and from no crate | `cli/runtime/tls.rs`, BUILD-AND-WATCH.md §1.1.2 | The two crate answers both lose on the bar's first clause. `webpki-roots` is a quarter-megabyte of *data this repository would ship*, pinning the trust set to a crate version rather than to the machine, so a user who installed a corporate root would have told their machine something a Buri program could not hear. `rustls-native-certs` reads the right thing and costs three transitive crates plus a link-time `Security.framework` on macOS — which a `staticlib` handed to `cc` by `build/link.rs` cannot carry without every artifact's link line growing a flag. Reading `/etc/ssl/cert.pem` and its four Linux spellings is forty lines. What it does not do is read the macOS keychain, and that is what `SSL_CERT_FILE` is for — the spelling OpenSSL, `curl` and `git` already honour, so it is one a user has met. Not cached across calls: a `OnceLock` would save a millisecond against a handshake that costs tens and would make "which roots" depend on when in the process the first request happened. |
| The runtime crate's own unit tests are **run**, by a test in the toolchain's suite that shells out to `cargo test` | `cli/tests/native/runtime.rs`, `cli/build.rs` | `cli/runtime` is a package that is deliberately not a workspace member and whose manifest is deliberately not named `Cargo.toml`, so `cargo test -p buri` could not reach inside it and no CI step did either — fifty-odd assertions about the float formatter, the UTF-16 comparison, the handle table and the allocator had been written and never executed. The `https://` slice needed a home for a test that starts a `rustls` server, and the only `rustls` in this repository is inside that package: the toolchain's own crate cannot grow a dev-dependency on it without failing `dependencies_stay_behind_the_bar`. So the seam was built rather than the bar bent. The path comes from `cargo:rustc-env=BURI_RT_PKG`, written by the build script that assembled the package, and the nested `cargo` gets the same environment scrubbing the build script's does. |
| The runtime's cargo package is **assembled in `OUT_DIR`**; its manifest in the tree is `manifest.toml` | `cli/build.rs` §1, BUILD-AND-WATCH.md §2.2 | `cargo package` skips any subdirectory of a package that holds a `Cargo.toml` — unconditionally, ahead of `include`/`exclude`, in both its listers — so `cli/runtime/Cargo.toml` deleted the whole directory from the published crate and a registry `cargo install buri` failed in the build script with nothing to compile. `include = ["runtime/**"]` was tried and does not override it. Publishing `buri-rt` as its own crate is the other answer and remains available; it is a registry decision with a release process attached, and this one is a rename. What it costs is `cd cli/runtime && cargo build`, which no longer resolves — the build has one driver and it is `cli/build.rs`. |
| The archive is linked **only when the objects reference it**, and the question is asked of the objects rather than of the program | `build/link.rs`, `build/actions.rs::link_key_of` | The design proposed a reachability query of `missing_intrinsics`'s shape — a walk of `program.funcs` for intrinsic keys. It would have been wrong, and wrong in the direction that fails a link: a program's intrinsic keys are not the whole of what it asks the archive for. Both native entry points call `buri_rt_argv_init` and `buri_rt_flush` unconditionally, reference counting reaches `buri_rt_alloc`/`buri_rt_free`, a division emits `buri_rt_abort_div_zero`, `==` on `Str` emits `buri_rt_str_eq` — and none of those is a `FuncKind::Intrinsic` anything could walk for. Asking the emitted objects for the one symbol prefix `buri_rt_` needs no third copy of every emitter's structural calls and cannot go stale when one moves. **The measured consequence, stated rather than buried: the answer is `Linked` for every Buri program, hello world included, and no artifact got smaller.** Dead-stripping is still what decides the size (370 288 bytes for `main() { .Ok(()) }` against a 6 035 480-byte archive). What the decision does buy today is the `link` key: an artifact that names no archive no longer carries the archive's digest, so it stops being relinked when the runtime changes. The alternative that lost is leaving it a constant, which keeps that term unconditional and leaves the day an entry point stops needing the runtime as a change to the key format rather than a change to one answer. |
| The flake vendors **both** lockfiles into one directory, and asserts the archive it produced | `flake.nix`, BUILD-AND-WATCH.md §3.2 | Merging the *lockfiles* was the obvious reading of "one `importCargoLock` takes one `lockFile`" and it loses to nixpkgs' own hook: `cargoSetupPostPatchHook` diffs the vendor directory's `Cargo.lock` against `src`'s and aborts when they differ, so a merged lockfile has to be un-merged before the build starts. A vendored `directory` source is a flat set of `name-version` directories, so merging *those* is a symlink loop and leaves `Cargo.lock` the toolchain's byte for byte. Leaving the gap open was the third option and is what shipped for a day: `nix build` stayed green and produced a toolchain with no native backend, which is a different compiler under the same version number — hence `assert-runtime-archive.sh` in the derivation's `postBuild`, where the four systems `eachDefaultSystem` covers are all systems `cli/build.rs` supports and an empty archive can only be a bug. |
| The carrier entry ABI is **one signature in a third file**, fixed before its first caller | `backend/carrier.rs`, `backend/stencil/asm.rs`, `backend/llvm/emit.rs` | This is the opposite call from `rt.rs` §1's, and the asymmetry is the argument. That row refuses to export a *runtime* symbol before a backend has a call to emit, because only the runtime knows its shape. This symbol is the **backend's**, and two of them emit it under one name for one caller: if they disagree, whichever the scheduler calls first decides what the other meant, and nothing — not the compiler, not the linker — says so. So the shape is written down once, both emitters are built from it, and a unit test renders each side back out (`carrier::ENTRY.render()` against an LLVM `FunctionType`'s printed form) and diffs the bytes. What is *not* shared is what is behind the door: stencil takes a Buri data stack from the runtime, LLVM takes none. `state` is passed and unread until a call site exists to fill it — a parameter added later moves every call site there is by then. |
| A carrier's Buri stack is **`mmap`ped per carrier**; `main` keeps the `__bss` block | `cli/runtime/memory.rs`, `backend/stencil/asm.rs` | One 64 MiB `__bss` block with one `PROT_NONE` guard is right for one thread and wrong for two: a second carrier's frames would land in the first's, and the fault a runaway recursion is meant to take at the guard would be a silent overwrite of somebody else's locals. The alternative considered was making the *static* block per-thread — a TLS `__bss` array — which multiplies 65 MiB of address space by every thread a program ever starts, whether or not it enters Buri code, and which cannot be `mprotect`ed once because each copy has its own address. `main` is deliberately left alone: a program that never starts a carrier makes no system call it did not make before, faults nowhere new, and links no door (`-dead_strip` deletes it). The runtime declares `mmap`/`mprotect`/`munmap` itself rather than adding `libc`, because the dependency set is closed by an exact list — and the *compiler* already emits a `bl mprotect` into every artifact, so the symbol is not new. **Amended 2026-08-30 (G6):** the mapping is still kept on release, and the pages are not. Everything above a 256 KiB retained prefix goes back to the kernel when a block goes idle, which is 48 MiB of resident set on one carrier that recursed once and 127 MiB across four. What the prefix buys is that nothing a shallow entry touches is ever released, so the ping-pong workload refaults no page; a watermark word above it makes such an entry cost a load instead of a system call, and a floor of one decommit per 1024 releases is the backstop under the watermark's one failure mode (a missed decommit, never a wrong answer). The measured trade is 0.004 µs → 0.014 µs per entry into Buri code, against 8.4 µs for the decommit itself — which is a TLB shootdown across the carrier pool, so its cost is a property of how wide the pool is rather than of the range. **Amended 2026-08-30 (B9):** per *task*, not per carrier, and there are now two mappings of that size rather than one — the Buri data stack, and the machine stack the task's own control flow runs on. Both are decommitted when they go idle and both keep 256 KiB, at opposite ends, because what is retained is what a shallow user touches: a prefix on the data stack, which grows up, and a *suffix* on the machine stack, which grows down. Their hysteresis differs and the difference is a frequency: a data stack is acquired and released once per **entry into Buri code**, so G6's watermark and floor are what keep a shallow entry from buying a system call, while a machine stack is acquired and released once per **task**, which is rare enough that it is simply decommitted every time and handed to a pool capped at sixty-four. `main` still keeps the `__bss` block and a program that starts no task still makes no system call it did not make before. |
| `TestTasks.parallel` drives the **same closure trampoline** the real one drives, rather than being a Buri loop — **reason amended 2026-08-30** | `sources/host_testing.buri`, `backend/intrinsic_keys.rs`, `cli/runtime/testing.rs` | A Buri body was written first, and the reason recorded for rejecting it was that a method body has no name for the caller's context. That reason is gone: the context is a named parameter now (the row below), and `SerialTasks` in the conformance corpus is a Buri body that passes it. The decision stands on the reason that was always the stronger one — **the order is the runtime's**. What this double decides is which permutation runs, and a permutation of `0..n` with a fault plan and a call log is not something a Buri body can choose; going through `step_call` also puts the double on the boundary the program will ship on, with exactly one thing changed. What it costs is a third `Extra::Step` key and a `self` that is `Arg::Scalar` where `host.HostTasks.parallel`'s is `Arg::Dropped`, because a `TestTasks` is a handle and the runtime has to ask it which order this run schedules in. |
| `Self` is the **implementing type everywhere**, and a callback meant to receive the caller's context takes a named `ctx` parameter instead | `sources/effect.buri`, `docs/lang/effects.md` §10.6, `semantics/expressions.rs`'s `implementing_ty` | `Tasks.parallel` was declared `f: fn(Self, Int, A) => B` and documented as handing the step "the full context", and those are two different values: `Self` is what implements the effect, which through a `context { … }` grants that one effect and nothing else. The trampoline delivered what the signature said, so a step reading `c.nowMillis()` answered `0` natively and *threw* on JavaScript — two backends disagreeing, which is worse than either being wrong. Both readings were reachable and one rule had to win. **Redefining `Self`-inside-a-callback to mean the caller's context lost**, and the reason is SPEC 10.9: an effect is an ordinary interface and anyone may write a type satisfying it. A callback parameter typed "the caller's context" has a type no `impl` can name and no value an `impl` holds, so no implementation written in Buri could ever call its own callback — `parallel` would have been implementable only by the compiler's own trampoline, and every hand-written fake would have been a different mechanism from the one that ships. Naming it costs one parameter and one repeated argument at one call site (`ctx.parallel(ctx, …)`, inside `core/tasks`), and buys: the front end needs no special case, `rewrite_call_args`' `Self` correction stops applying to the one method that must not have it, the "opaque `Self` inside a generic body" hole `self-subst-fix.md` §7 names does not arise for this shape at all — `C`'s bounds are the caller's — and `SerialTasks` is a nine-line Buri implementation that honours the promise. The native cost is nothing: `ctx` is `Arg::Dropped` in both tables, so the C signature of `buri_rt_host_tasks_parallel` is unchanged, and the value reaches the step through the state record slot `StepCall.ctx` already described. `Listen.listen` keeps `fn(Self, Request)` deliberately, and what that now means in practice moved with the "a context has no methods" flag-day: every call goes through `core/net/server`, whose body is generic, and inside a generic body a `Self`-spelled handler is handed the *receiver* — so a program's handler receives the caller's context and `rewrite_call_args` is what makes the value agree. Inspecting the acceptor is the `impl`'s own privilege now, which is where `OneShotListen` does it. The `ctx` beside `self` still lands when `core/net/server`'s hooks need one. **Amended (F2):** the `ctx` it was waiting for arrived as the loop moving into `core/net/server`, which is the same commitment discharged a different way; see the handler-free `Listen` row below. |
| `TestTasks.parallel` drives the **same closure trampoline** the real one drives, rather than being a Buri loop | `sources/host_testing.buri`, `backend/intrinsic_keys.rs`, `cli/runtime/testing.rs` | A Buri body was written first and is the reason this row exists. It cannot be right: the step is handed the *caller's context* and a method body has no name for one, so what a body can pass is its own receiver — which is a different value of a different width, and natively that is a shifted argument list rather than a diagnostic. Going through `step_call` puts the double on the boundary the program will ship on, with exactly one thing changed: the order. What it costs is a third `Extra::Step` key and a `self` that is `Arg::Scalar` where `host.HostTasks.parallel`'s is `Arg::Dropped`, because a `TestTasks` is a handle and the runtime has to ask it which order this run schedules in. |
| A **seed is the order's own number**, not a generator's | `cli/runtime/testing.rs`, `backend/js/runtime.js` | `n!` orders, numbered from zero in lexicographic order, read out through the factorial number system. A shuffle is not invertible, so a report that named a shuffle's seed could only be replayed by re-running the same generator on the same platform — and the two backends here are Rust and JavaScript. This way `everyOrder`'s `k`th run and `seed(k)` are the *same sentence*, `anyOrder`'s default is a rank rather than a magic constant — the last one until D6, the suite's own content since (the row below) — and a content-derived seed is legal at every length because ranks wrap. |
| `everyOrder` reruns the body by a **self tail call the middle end emits**, not by a loop where blocks are called from | `middle/monomorphize.rs`, both runtime tables | `test.replay` sits beside E6's `test.leave` and for its reason: blocks are called from three places — two hand-written machine-code entry points and a string of JavaScript — so a loop there is three implementations of one rule, and one of them is a relocation golden. `ExprKind::Loop` is `middle::tail_calls`' own shape (it rebinds a function's parameters and is entered from outside) and a `test` block has no parameters, so the honest spelling of *run it again* is the call the language already has; `tail_calls` turns it into the jump. The hook also moves E6's watermark, which is the question `buri_rt_test_leave`'s comment left open: each run's plan is its own run's promise. |
| A task the fault plan fails **ends the block** | `sources/host_testing.buri`, `cli/tests/failing/every_order/` | `Tasks.parallel` answers `[B]` and every `B` comes from the closure, so there is no third thing a double can do with a task it refuses to run: it cannot answer, and it cannot skip and still fill the list. Ending the run is what a task that died would do for real, so the fault carries a **reason** where `FsFault` carries an `IoError`, and the report says it. The consequence is that neither half of the plan can be asserted from inside a block — the firing one ends it and the unconsumed one fails it — so the golden is where both live, as E6's post-condition already is. |
| The context a `buri_rt_*` call drops is named by **position, from the declaration**, not recognised by the argument's type | `backend/runtime_table.rs`'s `Entry::ctx`, `backend/stencil/rtcall.rs`, `backend/llvm/emit.rs` | The rule "a context argument is dropped, whatever it weighs" was right and its implementation asked the wrong question: *is this value a `Ty::Ctx`?* That is the same question only while every `C: Alloc` is instantiated at a `context { … }` record, and `C` is an ordinary type parameter with an ordinary bound — SPEC 10.1 says `<C: Alloc>` and `<T: Ord>` are one feature, and SPEC 10.8's attenuating `ReadOnly<C>` exists so that programs pass something that merely *implements* the effect. One of those spread to a leaf the C signature has no parameter for and shifted every argument after it one register down: a `TestAlloc` handle where `buri_rt_list_push` wanted a pointer, and a fault in `memmove` before the first test block. The alternative was widening the type test — "is it a context *or* an implementor?" — and it cannot be made correct: `[TestAlloc]` is an effect-carrying type too, and it is `list.push`'s *element*, so a type test either misses the case or eats the data. Position is the only question with one answer. The cost is a fourth column on 27 of 184 rows, checked against the declarations themselves (`the_context_column_is_the_declarations_ctx_parameter`) rather than against a second list, and cross-checked against `llvm/runtime.rs`'s `Arg::Dropped`, which had been saying the same thing positionally all along — which is why the LLVM backend had only the one place with no table row, `str.concat`, wrong. |
| `anyOrder()`'s seed is the suite's **action key**, the same hash the result cache is keyed on | `commands/test.rs`'s `seed_of`, `cli/runtime/testing.rs`, `backend/js/runtime.js` | D-10 asked how a seeded order and `Cache::put` can both be honest, and the answer is to derive the seed from the thing the cache already asks about: a hash of every source in the suite's closure and of the invocation that would build it. The two then move together **by construction** — the order a suite schedules in changes exactly when the verdict that order produced stops being reusable, and never on a run that changed nothing — where a clock or a generator in this position would store a record the next run could not have produced. The alternatives were a per-run random seed (the shape that poisons the cache: a suite that passed under one order, remembered as passing, re-run under another) and turning caching off for suites that say `anyOrder` (which prices the feature at a cold run of the suite that most wants it). What it costs is that **no corpus can pin an `anyOrder()` order any more**: the seed is a hash of the corpus's own bytes, so the three conformance blocks that pinned the reverse now name `seed(5)` instead, and what the two remaining `anyOrder()` blocks assert is what a content-derived order promises — that it *is* an order, and that the results are still the items'. Delivery differs by backend and does not have to agree: `BURI_TEST_SEED` for a native binary, one number for a suite and a per-block list for a batched binary so that a suite schedules the same way batched as alone; a spliced `$t.seed` on JavaScript, which is the path that writes its own artifact. Absent means the default D5 chose, the last rank, so a binary run by hand and the harness that drives the backends directly are unchanged. |
| The order sentence rides the record **beside `error`, not inside it** | `backend/js/generate.rs`'s `$run`, `cli/runtime/testing.rs`'s `note_failure`, `commands/test.rs`'s `parse_results` | The failure record is one wire format with two producers and one consumer, and the order a run's tasks completed in is a fact about the **run** rather than about the throw: the sentence is worth printing under `a task was failed by the plan: …`, which carries no rendered values at all, exactly as much as under a failed `assert.eq`. Putting it inside `error` would have made it a third side of a comparison that has two. It is elided where a block scheduled nothing — which is almost every block — so a suite that never says `tasks()` produces the bytes it always produced, and `Verdict::Failed` carries it as an `Option<String>` for the reason the variant carries the diff: a passing test has nowhere to put one. |
| An idle carrier stack is decommitted with a **fixed anonymous re-map**; a cached *block* is returned to `malloc` and never decommitted | MEMORY.md §5.4, `cli/runtime/memory.rs` | The row asked for `madvise(MADV_FREE`/`DONTNEED)` or `munmap`, and on the two platforms this runtime supports the first two do not work: measured on macOS 15, over a 64 MiB block with 8 MiB dirty, `MADV_FREE` and `MADV_DONTNEED` both leave the resident set at 9 781 248 bytes and only `mmap(MAP_FIXED\|MAP_ANON)` takes it to 1 654 784 — Darwin's `MADV_FREE` is an offer redeemable under pressure and its `MADV_DONTNEED` is a no-op for private anonymous memory. A fixed re-map is a decommit rather than a hint and means the same thing on Linux, so one mechanism is exact on both. `munmap` loses for the reason the block is on a free list at all: it takes the reservation and the `PROT_NONE` guard with it and the next entry buys both again, when address space is the free half. The *block* cache is the opposite call and for the opposite reason — its pages belong to `malloc`, which alone can know whether the chunk it carved them from is empty — so its rule is return, not decommit: a carrier drains its lists when it ends (4 224 000 bytes across sixteen carriers, previously lost for the life of the process) and sweeps out every size it has not asked for in 1024 operations. |
| The multi-threaded mark is set **per program**, not per value: a program `middle::rc::crosses_tasks` says can reach a task boundary marks every block it allocates, and one that cannot marks none | `middle/rc.rs`, `cli/runtime/memory.rs::finish`, MEMORY.md §5.1 | The design row points at `rc::sharing` — where a second reference comes into existence — as the same question the escape bit asks. It is not: `sharing` answers about *sites* and the mark is about the transitive closure of a *heap*. A `[Str]` handed to a step is a block whose elements the step counts, a `Str` in a closure's environment is a block two carriers count, and a block the runtime made itself is one the compiler never saw — so a per-value mark has to be a deep type-directed walk (`Helper::Walk`'s shape, which G5 generalises) and a shallow one is exactly the under-count MEMORY.md §5.5 forbids. The program-wide answer is the over-set end of that asymmetry and is sound by construction rather than by audit, because the *allocator* is what marks. It costs one relaxed load and an `or` per allocation (+0.15 % on a 7.9 ns allocate-and-free pair) on every program, and atomic reference counting throughout a program that uses `core/tasks` — the price §5.4 already puts on threads. Narrowing it with G5's machinery is an optimisation over an answer that is already correct. |
| **A marked block is never unique**: `buri_rt_unique_cap` answers `None` on the mark, whatever the count | `cli/runtime/memory.rs`, `backend/llvm/emit.rs` (`cat.uniq`), MEMORY.md §5.1 | G2 left the test unforked on the argument that a thread holding no reference cannot make a second one, so a count of one cannot move under the caller who read it — and flagged, in its own handover, that the argument assumes the caller *holds* the reference it is testing. A borrowed parameter does not, and that is the shape a closure's captured value has, so with the run baton gone several carriers read the same `1` off one list and each take the in-place licence. Failing the test on the mark is the over-set direction: it costs a copy where the count was right, and it is the difference between a wrong answer and a slower one. `two_steps_never_append_to_one_buffer_in_place` fails five times in five on a tree without the mark, deterministically, because the fault is aliasing rather than scheduling. |
| A suspended task is a **saved stack pointer**, not a thread: three hand-written `(arch, os)` blocks behind one `buri_rt_task_switch`, and `swapcontext` is refused | `cli/runtime/switch.rs`, `switch_{macos_arm64,linux_arm64,linux_x86_64}.s`, `cli/runtime/rt.rs` | `swapcontext` loses three times over and each is sufficient: it is not in the runtime's dependency set, which is closed by an exact list; Darwin's supported surface does not carry it; and where it exists it makes a `sigprocmask` system call per switch, which is the whole cost of a switch several times over. What is written instead is nineteen instructions on AArch64 and thirteen on SysV, because **the C ABI has already done most of the work at the call site** — every register that is not callee-saved is one an `extern "C"` call has already given up. Three files rather than one because the callee-saved set is a property of the ABI and the symbol spelling a property of the container; the two AArch64 blocks are one body under ELF's directives and Darwin's underscore, and `the_two_aarch64_switch_blocks_are_one_body` asserts it. Only the host's is ever compiled, so `the_three_switch_blocks_assemble_for_their_targets` builds all three with `cc --target=` and reads the machine out of each object's own header. Measured: a switch is 12.6 ns, against a channel send and a thread wake-up in microseconds; ten thousand parked tasks fall from *impossible* — `pthread_create` answers `EAGAIN` at 8 192 — to 164 MB of resident set on thirteen carriers. |
| The Buri data-stack free list is keyed by **task**, not by thread; a task's **machine** stack is mapped by this runtime at the same size, with its guard at the end it grows towards | `cli/runtime/memory.rs` (`stack_list`, `buri_rt_task_stack_acquire`), `cli/runtime/rt.rs` | B7 kept the list on the thread and its handover said what would change it: a parked task outlives the carrier it started on, so the block belongs to whoever is *inside* it and that is a task. `stack_list` is the whole of the change — the running task's list where a task is running, this thread's otherwise — and `buri_rt_stack_acquire`/`_release` did not move, change signature or change their nesting behaviour; it costs +1.5 ns on a 14.9 ns pair, once per entry into Buri code. The *machine* stack is the second half and it closes `reports/wave6-b7b8.md` §5.2: a carrier's recursion depth used to be 64 MiB of acquired Buri stack under the frame-threaded backend and a 512 KiB thread stack under LLVM, where a Buri frame *is* a machine frame. A task's machine stack is now `BURI_RT_STACK_BYTES` like its data stack, so the two backends have one number, and its `PROT_NONE` guard is at the **low** end because a machine stack grows down — the mirror of `asm.rs`'s, which is above because the Buri stack grows up. `CARRIER_STACK_BYTES` survives and bounds a scheduler loop that runs no Buri code at all. |
| A scoped arena holds **reservations, not values**: `core/alloc::scoped` maps and releases real pages, and the values built inside a scope stay on the reference-counted heap | MEMORY.md §7.2.1, `cli/runtime/memory.rs`, `sources/alloc.buri` | `Scoped<C>` forwards twelve effects and replaces `Alloc` with a bump allocator over its own `mmap`s, released in bulk when the body returns. It cannot hold Buri values yet for a reason that is not about arenas: the native ABI drops the context argument from every runtime call, so the operation that builds a list never learns which allocator asked for it (§7.3.1). Serving blocks from the arena before that is fixed would also need a deep copy at the scope's boundary, and without one the only sound release is never — an arena that is never released is not an arena. The interim rule is checkable in one line: `allocate` answers a `Region`, which carries the charge and not an address, so nothing the language can name points into an arena and the bulk free cannot dangle, whatever the escape bit says about any block. G5's `Helper::Copy` is what lifts it. **Amended 2026-08-31 (G5):** it holds the values. The gap that row named is real and is not closed by closing it — the ABI still drops the context — but the question can be answered without being asked, by making the arena a property of the carrier for the dynamic extent of `body`. The other half of that row's argument is now built: `Helper::Copy` deep-copies the answer onto the caller's allocator before the pages go back, so the bulk free is sound for values as well as for charges. What survives unchanged is the row's reasoning about direction: an over-set arena costs a copy and an under-set one costs nothing, which is why every allocation in the extent is the scope's rather than only the ones a `Scoped` was handed. |
| Inside a generic body, a **`Self`-spelled callback is handed the receiver** — `middle/monomorphize.rs` adapts the call rather than retyping the handler | `middle/monomorphize.rs`'s `rewrite_call_args`, `sources/effect.buri`, `cli/tests/native/agreement.rs` | `Self` is the implementing type, and `implementing_ty` reads it off a context's bindings wherever the front end can see one. Through a bounded `C` it cannot, so `Self` there is `C` — right when `C` is an implementation, wrong when `C` is a context. Monomorphization used to correct that by *retyping* the handler down to the implementation, which rewrote the context's type through the whole argument subtree: an effect wrapper's `Scoped(c, arena)` became a `Scoped<`acceptor`>` and was passed to a closure built at `Scoped<C>`, and both native backends read past it. It adapts the call instead — the acceptor's own `Self` is dropped at the boundary and the receiver is passed — so a handler in a generic body is handed a value satisfying every bound `C` declares, which the acceptor need not. Where the receiver *is* a `context` value, `Self` is still the implementation and a handler may read its fields. |
| A value that leaves a scope is **deep-copied**, by a per-type walk generated exactly like the release walk | `stencil/glue.rs`'s `Helper::Copy`, `llvm/emit.rs`'s `Job::Copy`, `cli/runtime/memory.rs`, MEMORY.md §7.2.1 | `core/alloc::copyOut` is compiled, not called: each backend generates the copy for the concrete `T` at the call site, which is what repairs the erasure a generic intrinsic's key suffers. The division is the slice: everything that depends on a *type* is in the walk, where the type is known, and everything that depends on a *block* is in the runtime, where the header is — the archive is compiled once against no Buri type, so a copy that had to know a layout could not live in it. A copy is **not a share**: nothing in either walk increments a count, so the answer's blocks are fresh and uniquely owned and the source's counts do not move, which is exactly what a bulk `munmap` needs. A closure costs one word for it — `Ty::Fn` is as silent about a copy as about a release, so the environment block carries both functions (`ENV_FIELDS` is 16); the alternative, one word pointing at a static pair per type, was rejected for putting a second load in front of every drop of every closure. |
| A scope's allocator is **dynamically scoped to the carrier**, not passed | `cli/runtime/memory.rs`'s G5 section, `sources/alloc.buri`, MEMORY.md §7.2.1 | G4 held the arena to charges because the native ABI drops the context argument from every runtime call, so the function that builds a list cannot be told which allocator asked. It is not told: `scoped` makes its arena the one `buri_rt_alloc` serves from, for that carrier and for the dynamic extent of `body`. That is an over-approximation of "charged to the `Scoped`" and it is the safe end of MEMORY.md §5.5's asymmetry — a block that should have been on the heap and is in the arena leaves with the answer or dies with the scope; one that should have been in the arena and is on the heap merely misses the optimisation. Neither dangles, because a value's lifetime never exceeds the extent it was made in except by being the answer, and the answer is copied. The slot is per **carrier** and `rt.rs` saves it across a stack switch, so a task started inside a scope allocates on the platform heap — the safe direction again. The cost is `+3.7%` on the allocation-heaviest program in the tree and **zero** structurally for a program that never opens a scope (PERFORMANCE.md §6.7); a scope itself costs 183 ns, of which the 2.4 µs of `mmap`/`munmap` was removed by an eight-block pool. |
| **`core/testing/context` is deleted, and `core/host/testing` is the only test platform.** The module, `Hermetic`, the thirteen free functions, `ReadOnly<C>`, 37 rows in each native runtime table, 40 JavaScript bodies and 800 lines of the archive. |  | The coexistence window was waves 3–7 and it has closed: every corpus moved in E7–E11 and every double has a twin. A second spelling of a shipped platform is a second thing to keep agreeing with the first. |
| **`semantics/effects.buri` moved with the module rather than dying with it.** 134 blocks migrated; its subject is SPEC 10 and 11.3, not the module that used to supply its doubles. |  | A file held back for six waves *because* it was the old module's conformance file is not a file about the old module. What it asserts — spreads, inheritance through a spread, freshness, `Self`, a step's context — is language behaviour that survives. |
| **The nine-binding world a spread needs is a `context Defaults` in the test source.** `core/host/testing` ships doubles, not an assembled world. |  | H3's rule is that a test binds what it needs; the exception is a file whose subject is *what a spread carries*, and that file writes its own base rather than asking the standard library for one. |
| **One conformance block is deleted: `readOnly` over a caller's own `Fs`.** |  | Its subject is exactly the deleted surface — a generic `ReadOnly<C>` over any `Fs`, which E2 folded into a method on `TestFs`. A test that wants a generic attenuator writes one; `//lib/semantics`'s `Guarded<C>` is that, and three neighbouring blocks exercise it. |
| **`cli/tests/migrate.rs` and `harness/migrate.rs` are deleted with the module.** |  | The rewriter's entry point is a file importing `core/testing/context`; with no such file possible, every case constructs a source that cannot compile or sweeps for a string that cannot occur. Its own closing note set the standard: delete when the behaviour is unwanted, not when it is inconvenient. |
| **`nothing_is_skipped_and_nothing_is_written_without_a_runner` takes `ONE_RUNNER`.** |  | `buri_rt_test_enter` resets the per-process run counter `everyOrder` walks, so a case that enters a block writes what `the_body_is_run_once_per_order` reads. One failure in eight runs without the guard; none in eighteen with it. A pre-existing race, fixed rather than retried. |
| A surface is named as a module; every other module is named by its file | `lang/modules.md` §4.1.1, `build/libraries.md` | A surface is the one thing a package publishes, so naming it is naming the package — `//lib/money` is both the `dependencies` label and the import path, the one point where the two namespaces are the same string. Everything else is one file among many and the path has to say which. The two cannot be told apart by shape, which is why `import-path-without-a-file`'s fix is resolved from the layout rather than spelled from the string. |
| A package's own test source names its library the way a dependent does | `build/testing.md`, `lang/modules.md` §4.1.1 | Nick's call. The alternative — a rule phrased around where the writer stands — gave one library two spellings, and the odd one belonged to the file that tests it. It also cost more: `check_import_legality` asks a *kind* rather than a side of a boundary, three internal-import diagnostics share one fix, and the rewriter stopped needing to know who is importing. |
| `//pkg/lib.buri` stays accepted and canonicalises | `lang/modules.md`, `build/libraries.md` §"the long way round" | Removing the aliasing is not a deletion but a **new refusal** — code, page, fixture, docs. And the canonical path is load-bearing beyond the two spellings: `package_relative_source`, `is_declared_test_source` and the rule-boundary lookup all read a *file name* off `PackageModule::path`, so it is what keeps a module's identity a file while its name is a package. Handing any of them a `//lib/money` is a check skipping in silence. |
| A binary's entry point is a file, not a surface | `lang/modules.md` §4.1.1, `build/testing.md` | `//cmd/app` would be that package's `lib.buri`, and a package with only a binary has not got one. So `//cmd/app/main.buri`, from that binary's own test sources and nowhere else. |
| `GENERATOR_REVISION` 6 | `design/PERFORMANCE.md` §6 | Revision 5 gave back. Every generated module imports four or five standard library modules across a boundary, so every import line is nine bytes shorter; nothing inside `//bench` moved, which is why three corpora came back byte-identical where revision 5 moved all eight. |
| An effect's methods are called through the module that wraps the effect, never on the value that carries it | `semantics/expressions.rs`'s `report_effect_method`, `standard_library/mod.rs`'s `WRAPPERS`, `docs/lang/effects.md` §10.2, `docs/lang/expressions.md` §6.7.3 | A context is the set of things a program may do, and a receiver is the smallest, quietest part of a call — an effect performed through one reads like a method on ordinary data. Passing the context as an argument puts the authority where the reader is already looking. It also dissolves the ambiguity hazard `sources/effect.buri`'s `Sockets` doc argues at length: method lookup through a bound searches every bound effect, so two effects claiming one verb make it unusable for both (`Ui.read`/`Watch.read` is the shipped casualty), while a module-qualified call cannot be ambiguous. The gate is one predicate in the one place a method reaches a bound, plus the `Ty::Con` arm for a method an `impl` supplied. Two layers stay below it: the standard library, which is where the wrappers are, and an `impl` supplying an effect, which is where the operation is implemented and which cannot delegate to a wrapper whose bounds it does not carry. |
| Every method of every declared effect has a standard-library function that calls it, checked | `standard_library/mod.rs`'s `WRAPPERS` and `every_effect_method_has_a_door` | Once the method form is refused outside `core/*`, a method with no wrapper is an operation nothing can perform. Six of thirty-eight were in that state and nothing said so. `core/proc` and `core/net/server` exist because of this check rather than because anything calls them — `core/net/server` in particular is a *door*, not a design: no platform grants `Listen` or `Sockets`, and F2 grows the vocabulary. |
| `Stdout` and `Stderr` answer `Result<(), IoError>` | `sources/effect.buri`, `core/io`, both runtimes, `runtime_table.rs`, `llvm/runtime.rs` | A closed pipe, a full disk and a revoked permission are things that happen to a print, and `()` claimed they do not — the failure surfaced only as `main`'s exit status, which is the one place a program can no longer act on it. Output is buffered, so the honest promise is "the first failure reaches the next caller", and both runtimes keep it with a held error rather than pretending the buffered write is the real one. The cost is a two-arm match per print, `Result.ignore()`'s body, on every backend. |
| `discarded-result` stays total: a dropped print is reported like any other | `commands/lint.rs`'s `check_discarded_results`, `docs/lints/discarded-result.md`, `repositories/linting/discarded_result` | The row above makes nearly every line a program writes a `Result`, so the rule now reports printing as well as everything else — a hello world's `buri lint` exits 1. An exemption for `core/io` was tried and **Nick reversed it knowing the cost**: `ignore` is the one spelling of a deliberate drop, and a rule that let the commonest fallible call out of the report would stop meaning "somebody decided". What the exemption was hiding is written down instead — 101 prints across 87 repository fixtures answer for themselves with `mapErr`, and the three lint fixtures whose printer returned `()` now answer `Result<(), IoError>` and let the caller decide, which is the honest shape for a helper that has nowhere to put a failure. |
| A handler reached through a wrapper is handed the caller's context | `core/net/server`, `middle/monomorphize.rs`'s `rewrite_call_args`, `conformance/lib/semantics/test/effects.buri` | `Listen.listen`'s `fn(Self, Request)` is reachable only through a generic wrapper body now, and inside one a `Self`-spelled handler receives the receiver — the rule `rewrite_call_args` already implemented and `design/native/DECISIONS.md` already stated for the generic case. It is now the only case. The corpus asserts it from the side that can only be true if the value is right: the handler uses an effect the acceptor does not grant. `Self` is still the implementation wherever the implementation's type is in view, which is the `impl` body and the front-end tests. |
| A comment written at the end of a line stays there | the formatter's `Doc::Aside`, `cli/tests/formatting` | The formatter puts every other comment on a line of its own, above what it was written above. The aside is the exception because it is *about* the code beside it — and it is not measured, so the layout of a line never depends on the sentence after it. Rejected alternative: keep moving them, which the documentation's `// ERROR:` convention cannot survive. |
| One command formats every Buri anybody wrote | `buri format` / `--check` | `buri format` lays out markdown fences and the examples in `///`/`//!` comments as well as sources and build files, and `--check` gates all of it. A repository whose sources are formatted and whose examples are not has two house styles, and the one a newcomer copies is the one in the prose. |
| `buri docs test` checks layout as well as truth | `buri docs test` | Any repository gets fence enforcement with nothing to configure. |
| The repository-wide format gate walks the tree, and exemption is a written row | the suite's format gate | A corpus that arrives tomorrow arrives enforced; an exemption names a corpus whose subject is unformatted input and says which suite asks it its question. |
| A saved benchmark corpus is output, not source | `cli/benches/corpora`, its digest pins | Exempt from the format gate, pinned by its digest. Making the generator emit four spaces is a revision bump and a re-record, deliberately not folded into the sweep. |
| A generated benchmark corpus is laid out by the formatter, not by the generator's `format!` strings | `generate.rs` (its last act on every module is `formatting::source`) | Rejected alternative: emitting four spaces from the ~35 emission sites — it cannot be made correct, because the printer also sorts import runs, hoists `derive`s above the declaration they are about, and breaks a line at a width that depends on how long the generated identifiers came out. A generator spelling that by hand is a second, worse copy of `formatting`, right for the eight fixed corpora and wrong for the twenty parameterised ones. |
| A change to `formatting` is a change to the generator, and takes the revision-bump ceremony | stated in four places, `design/PERFORMANCE.md` §6 among them | The alternative — formatting only in the recorder, so the pins stay over unformatted bytes — was rejected: it would make `manifest.txt` say `profile = mixed` about bytes the `mixed` profile does not produce, and leave the pinned tier measuring source nobody would check in. |
| A benchmark corpus is inside the repository's layout gate, not exempt from it | `cli/tests/language/corpus.rs` | Measured: 70 files and 551 KiB cost 40 ms in a 0.3 s test. An exemption is a claim that a check is too expensive; at 40 ms there is no claim to make. |
| Generated output is checked like every other file and *fixed* differently | `GENERATED_NOT_BLESSED` | A drift under `cli/benches/corpora` is reported with `--record`, and `BURI_BLESS` leaves the file alone. Blessing generated output writes bytes no generator wrote and turns a readable diff into a hash mismatch three minutes later. |
| `GENERATOR_REVISION` 7 | `design/PERFORMANCE.md` §6 | The first revision that moves `lines` and `modules` and not only `bytes`. Defensible because the move is +0.32% on the anchor and under 0.5% on 38 of the 40 pinned corpora, so the lines/s series is unbroken in practice; and worth making because a line rate quoted over source nobody would check in divides by the wrong denominator. |
| **`effect Listen` carries no request handler; the accept loop is `core/net/server`'s, in Buri** | `sources/effect.buri`, `sources/server.buri`, `cli/runtime/net.rs` | The effect used to take `onRequest: fn(Self, Request) => Response` and invoke it, which made the acceptor the thing that entered a program's own code and handed the handler a value granting only acceptance — `core/effect` already recorded that it would have to gain a `ctx: C`. Four operations and a Buri loop is the same fix one level up and costs the effect a parameter instead: the handler runs under the caller's own context, so it may allocate, print or start a task without any of that crossing the effect; the loop is a tail call and costs no stack (SPEC 15.1); the runtime keeps no Buri value between calls, which is what lets the acceptor be a blocking implementation rather than a scheduler; and no runtime-driven closure key was needed, so `intrinsic_keys.rs`'s trampoline and both backends' emission are untouched by an entire server landing. Lost: the standard library's last `Self`-spelled callback, and with it the end-to-end guard on `rewrite_call_args` — which keeps its unit tests and three front-end snippets that declare an effect of their own. |
| **A server's error is a struct — `ServeError { cause, detail }` — and not a `NetError` variant** | `sources/effect.buri`, `cli/runtime/net.rs` | `cli/runtime/lib.rs` §2.1 names an enum error by a variant index and restricts that variant to carrying no fields; a non-enum error crosses whole through an out-pointer of its own. `NetError`'s `BadUrl(Str)` and `Transport(Str)` are exactly why `host.HostNet.fetch` has a body in the archive and no row in either table, and a new error declared the same way would have inherited that wall on the day it was written. Two fields also keep two questions apart: `cause` is what a program branches on, `detail` is what it prints. Lost: one enum where there are now two, and the `?` that would have mixed a client's failures with a server's. |
| **The acceptor frames HTTP/1.1 itself; `hyper` stays unlinked** | `cli/runtime/net.rs` | `http.rs`'s argument, from the other side of the wire: a synchronous exchange over one connection at a time is the whole of what is being asked for, and reaching a framing layer through `hyper` would mean standing up a reactor per connection to get at something two hundred lines already do. It also keeps `.github/scripts/assert-runtime-archive.sh` true as written — `hyper`, `tungstenite` and `quinn` are still absent on every leg and the size budget did not move — so the slice that first drives `hyper` is the one that moves that line, as B6, C7 and C4 each moved theirs. Lost: h2, ALPN and keep-alive, which are the same slice as the task pool. **Amended (F3):** The row named "the day handlers run on tasks of their own" as the day to take the decision again. It was taken again and came out the same way: a worker per handler is a fan-out over the same accept, the same head parser and the same response writer, none of which held state between calls, so the framing did not move. What is genuinely `hyper`'s — multiplexing two requests over one connection, and negotiating which protocol that is — lands with TLS in F4, and that is the commit that moves `assert-runtime-archive.sh`'s absent list. |
| **A handler runs on a worker from a pool, and the pool is `Tasks.parallel` over the accept loop** | `sources/server.buri`, `sources/effect.buri`, `cli/runtime/net.rs` | `effect Tasks` has no detached spawn — every task has finished before `parallel` returns, which is what keeps a task from outliving the context that granted it — so "answer this one somewhere else and come back for the next" is not expressible. Running several whole accept loops is, and it gives bounded concurrency a program can read off `Listener.handlers` instead of an unbounded one whose worst day is decided by its clients. The cost is `Alloc + Tasks` beside `Listen` in `serve`'s bound, which is authority made visible where this language puts authority; a program that grants `Listen` and not `Tasks` does not compile. One handler is no fan-out at all, so a test double and the frame-threaded backend both get exactly the server this module was before. Lost: the design note's example, which binds only `Alloc` and `Listen`. |
| **`listenAccept` answers a connection, `listenRequest` reads it, `listenRespond` names it** | `sources/effect.buri`, `cli/runtime/net.rs` | With one worker, "the request the accept last handed out" named exactly one connection and a listener was identifier enough; with a worker per handler there are as many outstanding requests as there are workers. Accepting and reading are two calls rather than one because only the first waits — the accept is where a server spends its idle life and is what a shutdown must interrupt, while the read is of a request the acceptor already framed and holds — and because folding them would make every hand-written implementation build a `Request` before it could say which connection it accepted. Lost: one call per request became two. |
| **How many handlers a listener hosts is the acceptor's number, on `Listener`** | `sources/effect.buri`, `sources/server.buri`, `cli/runtime/net.rs` | `listenBind` spends all ten of `MAX_INT_ARGS`, so a program's preference has no way across and a `Server.concurrency` field would have been a knob that silently did nothing — the same argument that makes `headerTimeoutMillis` and `bodyLimitBytes` the runtime's constants. It rides on the bind rather than a sixth effect method because a sixth method claims a sixth name out of the namespace every effect bound beside `Listen` shares. The runtime's number is a constant sixty-four rather than a function of the processor count, because a handler waits far more than it computes and because a number nobody can pin should at least be one everybody can predict. `run` believes it only inside its own ceiling, since a `Listener` is inert data a program can forge. Lost: a program cannot bound its own concurrency until the bind's budget grows. |
| **`core/actor`'s loop is Buri's; the runtime holds the mailbox, the state and the reply slots and never calls a step** | `sources/actor.buri`, `cli/runtime/rt.rs` | The design row put the state in the runtime and the step behind a closure the runtime calls between two calls of its own. `Extra::Step`'s record is allocated on the **call site's frame** in both native backends (`stencil/rtcall.rs::step_extra` writes it past `st.frame.size`; `llvm/emit.rs`'s `Arg::Step` is an entry-block `alloca`), and `start` returns before the first step runs — so a runtime-held step is a use-after-free on both, not a stencil-only gap. Moving the loop into Buri is F2's move one level further and needs no backend machinery at all: nine data-only entries, no `intrinsic_keys.rs` row, no `Extra` shape, no emission changed. It also satisfies the row's own "state is a local of the actor's carrier, threaded through `step`" more literally than a Rust loop would — `draining` is a self tail call and the state is its parameter. Lost: an actor does not run between the calls that drive it. The concurrent arm needs a heap-owned step record *and* B7's per-carrier Buri stacks, and no signature in `core/actor` moves on the day they land. |
| **A `[T]` is the carrier: the block itself is a fifth way to cross an erased type** | `middle/monomorphize.rs`'s `GENERIC_INTRINSICS`, `backend/runtime_table.rs`, `cli/runtime/rt.rs` | The four established carriers are a stride, an address, a descriptor and a generated thunk, and `core/actor` needs none of them: everything it moves is a one-element `[Carried<T>]`, which is `{ ptr, len }` whatever `T` is, and the runtime only ever queues it and hands it back. So nine generic keys reach the archive with no type information at all and none is needed — the runtime never indexes, copies or frees one. The cost is one word per carried value (`Carried<T>`'s `pad`), which is what keeps a zero-width element from producing an empty block that `list.get` answers `.None` on and that niches `Option<[T]>` to `.None`. Rejected: `Extra::Element`'s stride and glue, which would have described an element nothing looks at. |
| **`lib.rs` §3 gains one exception: an actor entry keeps the pointer it was passed** | `cli/runtime/lib.rs`, `cli/runtime/rt.rs` | "A runtime function never stores a pointer it was passed" cannot hold for a mailbox, which is by definition a place a value waits between two calls. The exception is named, scoped to the nine, and paired with what makes it sound: an entry that keeps a block takes a reference and gives that reference away when it hands the block back, and **every** block taken is handed back — to a step, to `stop`'s discard loop, or to the close that answers the final state. `Held` is the type that carries the rule; `stencil::nothing_is_leaked` and `the_glue_balances` are what would notice if it stopped balancing. |
| **`core/actor` names its default mailbox, where `core/net/server` deliberately does not** | `sources/actor.buri`, `cli/runtime/rt.rs` | F2's convention is that `.None` is the absence of a choice and the runtime turns it into a default, and it holds there because nothing but the runtime needs to know what the default was. Here `send` is what enforces the bound — it runs the mailbox down when a post reaches it — so a bound only the runtime knew would be a bound the driver could not respect, and an actor that filled up would wait for a drain nobody was going to do. `MAILBOX = 64` is therefore written in both places and `the_default_mailbox_is_the_one_core_actor_names` reads both sources and compares. Lost: one instance of a convention worth keeping, and the exception is stated where the constant is. |
| **`core/actor`'s operations are module functions keyed `actor.*`, not the methods of an effect** | `sources/actor.buri`, `standard_library/mod.rs` | SPEC 10.2 is about reaching *the outside world* through a context, and a mailbox is neither the outside world nor something a test would want a second implementation of — the row itself says `core/actor` needs no test double. So the nine are bodyless `fn`s with the authority in their bound (`C: Tasks`), which is exactly `core/list`'s shape for its allocating combinators. What that buys: no `effect.buri` declaration, no `HostActors`, no grant-table row, no `TestActors` in `core/host/testing`, no nine extra `host_testing.*` symbols in two runtime tables, and no `WRAPPERS` rows — `every_effect_method_has_a_door` is untouched and `core_actor_declares_no_effect_and_so_needs_no_door` is the assertion from the other side. Lost: an actor cannot be faked by binding a different implementation; testing one is calling its `step`, which is the row's own answer. |
| **`alloc.copyOut` is exported** | `sources/alloc.buri`, `sources/actor.buri` | G5 left the copy-out walk, its two runtime entries and its intrinsic key in place and said the F-family's boundaries were "the same call at a different site". The call site is `core/actor`'s `send` and `Reply::answer`, and reaching it needed the function to stop being private to `core/alloc`. A second `copyOut` declared in `core/actor` would have been a second key for one generated walk. Lost: one name added to a public surface, for a function whose answer is equal to its argument at every depth on every backend. |
| Union, not "swap tracked for a filesystem walk" | `cli/tests/language/corpus.rs` | The tracked-only walk was bought to keep `target/` out — four CI jobs had gone red on scratch repositories the suites materialize under it while they run. `--exclude-standard` keeps that guarantee (`target/` is gitignored) and pays for it with a second `git` call, not with a hand-rolled `.gitignore`. |
| Drop entries ending in `/` | `cli/tests/language/corpus.rs` | `git ls-files --others` reports a *nested checkout* as the directory itself rather than looking inside. On this machine `.claude/worktrees/` holds other agents' linked worktrees, full of half-finished sources; a checked worktree really did prove to be a directory entry, not a thousand files. The filter states the contract ("this returns files") and the test pins git's behaviour, so a future git that recursed would be caught here rather than in a red main checkout. |
| The proof repository is a `harness::Scratch`, not a probe file dropped in the real tree | `cli/tests/language/corpus.rs` | An unformatted `.buri` written into the checkout is visible to `every_source_in_the_repository_is_formatted` running in a sibling nextest process — the new test would have made the gate flaky, which is exactly what it is defending. `Scratch` roots under `CARGO_TARGET_TMPDIR`, which *is* inside the checkout when `CARGO_TARGET_DIR` is, so the ignored ground is still the ground being tested, and its `Drop` removes it. |
| One test asks all four questions | `cli/tests/language/corpus.rs` | Committed / never-added / under `target/` / inside a nested checkout are one enumeration contract. Split across four tests they would each rebuild the same repository. |
| **A `let` binding nothing reads is dropped after its initializer's own reference operations, not before them** | `middle/rc.rs` | A `let`'s initializer is scanned at the statement's own node, so the drop of an unread binding and the `incref` that gives that binding its count land at one `(node, After)` key, where sites run in push order. Pushing the drop first made every such pair *release then retain* — a free followed by a write through the freed header, which the block cache is using as free-list storage, so the crash surfaces at an unrelated allocation later. The counts balanced the whole time, which is why `check_balance` never saw it and why it read for a wave as an LLVM miscompile (`reports/wave9-f3.md` §5). The ordering now matches what the `Stmt::Expr` case beside it always did. Lost: nothing; the same drops are planned at the same node for the same bindings. |
