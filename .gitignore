target/
.buri/
out
# Zed rebuilds these from the grammar pin in extension.toml.
editors/zed/grammars/
# `nix build`'s symlink to the store path it produced.
result
result-*
# macOS and direnv droppings.
.DS_Store
.direnv/
# Machine-local Claude Code state.
.claude/settings.local.json
.claude/scheduled_tasks.lock
# Kept locally, not distributed: vendored papers and the blog draft.
reference/*.pdf
blog/
.notes/
CLAUDE.md
AGENTS.md
